# PROGRESS 4 — Ambientación Natural del Hero

> Checklist espejo de [`plan4.md`](./plan4.md). **Documento activo de tracking.** `PROGRESS.md` (ronda 1) y `progress2.md` (ronda 2) quedan cerrados. `progress3.md` (ronda 3) queda **parcialmente cerrado**: sus Fases 0, A y B están completas con evidencia y no se vuelven a marcar; sus Fases C, D, E y F siguen sin comenzar y su destino se decide en `plan4.md §3.4` — lo que esta ronda no absorbe se remite desde la Fase K y **sigue vivo en `progress3.md`**.
>
> **Convención de estados:** `[ ]` pendiente · `[~]` en curso / condicionado · `[x]` completo · `[!]` bloqueado
>
> **Regla de marcado (contrato anti-cosmético, `plan4.md §7`):** ningún ítem se marca sin evidencia adjunta en su propia fila — captura a un % de scroll específico, medición numérica, o assert automatizado. "Debería estar arreglado" no es evidencia. Si un fix no alcanza el criterio, el ítem **queda sin marcar** y se registra por qué, en vez de rebajar el criterio.

---

## Estado global

**Fases 0₄, G y H completas, con evidencia (sesión de implementación 2).** Fase 0₄/G: ver sesión de implementación 1. Fase H: los 5 ítems (H1-H5) cerrados — bosque de coníferas instanciado en tres bandas (campo compartido `terrainField.ts`), pasto rehecho con normales explícitas y viento, Mobile Low con vegetación real, y el avión verificado sin oclusión mediante un descarte geométrico nuevo (`forestCameraStandoff.ts`) que un spike de esta misma sesión encontró necesario — ver H5. Fases I, J: no comenzadas.

Carencias de `plan4.md §2`: **#15, #16, #17, #18, #19 cerradas** con evidencia (Fase G/0₄/H). **#14 sigue abierta** — es de la Fase I, siguiente en la secuencia.

---

## Línea base

> La sesión de planificación de esta ronda **no pudo correr el sitio en un navegador** (contenedor sin `node_modules`), así que dejó todo lo de esta sección como **heredado** de `progress3.md` o **derivado** a mano, sin valor de evidencia (`plan4.md §7`, punto 9), y por eso existió la Fase 0₄: remedir en el navegador antes de que ningún umbral se dé por bueno. **La sesión de implementación 1 ya hizo esa re-medición** — la tabla de `horizonStepEstimate`/`groundDetailEnergy` más abajo es `[medido]` en navegador real, no heredada ni derivada. Lo demás (draw calls/triángulos heredados de la Fase B, y las derivaciones geométricas de §1.3-§1.5) sigue con su etiqueta de procedencia original.

Draw calls y triángulos vigentes tras las Fases 0/A/B `[heredado de progress3.md]`:

| | hero desktop-high (0.01) | hero mobile-low (0.01) |
|---|---|---|
| draw calls | **37** / 250 | **8** / 100 |
| triángulos | 86.304 / 1.500.000 | 83.842 / 500.000 |
| margen libre de draw calls | 213 | 92 |

Otras cifras vigentes `[heredado de progress3.md]`:

| Métrica | Valor | Origen |
|---|---|---|
| `clippedWhitePct` de `01-hero.png` | 0,496 % (`<2 %`) | Fase A6 |
| `panelContrastEstimate` de `03-spec-sheet.png` | 19,81 (`≥4,5`), probado independiente del fondo 3D | Fase 0 · ítem 03 |
| Ratio de medias / medianas de HDRI | 1,923× / 2,34× (ambos `<4×`) | Fase B3 |
| Payload bloqueante S0 | 9,79 MB de 15 MB | Fase B4 |
| Tone mapping efectivo en runtime | `ACES_FILMIC`, 20/20 capturas | Fase A1 |
| `scene.environment` nulo | 0 de 103 puntos de scroll | Fase A4 |
| Piso de ruido de SwiftShader entre dos corridas idénticas | 0,029 % de píxeles (umbral >5/255) | Fase 0 · ítem 02 |

Derivaciones de la auditoría de esta ronda, **todas pendientes de confirmación en el navegador** `[derivado]`:

| Métrica | Valor derivado | Dónde se usa |
|---|---|---|
| Niebla de S1 (`ρ=0.0015`): `F(d) = 1 − exp(−(ρd)²)` | 0,086 a 200 u · 0,555 a 600 · 0,895 a 1000 · 0,988 a 1400 | `plan4.md §1.3`, §1.6 |
| Píxeles por unidad en S1 (1440×900, fov 45) | `1086,5 / d` | `plan4.md §1.4` |
| Alto en pantalla de un pino de 12 u | 326 px a 40 u · 130 a 100 · 52 a 250 · 13 a 1000 | `plan4.md §1.4`, §3.2 |
| Alto en pantalla de una mata de pasto (0,55–1,6 u) | 15–43 px a 40 u · 4–12 px a 150 u | `plan4.md §1.5` |
| `n.y` de la normal lateral de un cono `r=1,6 h=4` | 0,37 | `plan4.md §3.2`, §3.4 |

Línea base real medida en navegador por la Fase 0₄ (reemplaza la fila anterior de "sin medir"), `[medido]`, árbol previo a cualquier cambio de Fase G:

| Captura | `horizonRow` | `horizonStepEstimate` | mediana local | **ratio** | `groundDetailEnergy` |
|---|---|---|---|---|---|
| `01-hero.png` (0,01) | 365 | 23,65 | 0,177 | **134,0×** | 3,57 |
| `02-takeoff.png` (0,19) | 202 | 9,93 | 0,210 | **47,2×** | 2,80 |
| `08-mobile-hero.png` (0,01) | 412 | 32,38 | 1,838 | **17,6×** | 4,92 |
| `08b-mobile-taxi-13.png` (0,13) | 348 | 119,95 | 0,333 | **360,2×** | 4,08 |

Confirma cuantitativamente el bug #14: el criterio de cierre de I4 (`horizonStepEstimate ≤ 2×` la mediana) tiene que bajar el ratio de hoy en 1–2 órdenes de magnitud. VRAM de textura por tier: **medida y declarada**, ver 04-04 (16,78 MB high/mid · 4,19 MB low).

---

## Carencias encontradas en la auditoría de la ronda 4

Ninguna reportada por el usuario; todas salieron de leer el código y calcular. Detalle y criterio de cierre en `plan4.md §2`.

| # | Sev. | Carencia | Estado | Evidencia |
|---|---|---|---|---|
| 14 | Alta | **El domo de cielo no participa de la niebla** (`skyDomeMaterial.ts:30`, `EnvironmentPlaceholder.tsx:113`) — el "borde recto" es un salto de **color**, y no lo arregla ninguna cantidad de terreno nuevo | [ ] | Fase I1/I2/I4 — sigue abierta, es de Fase I. Cuantificada en 04-01: ratio 17,6×–360,2× la mediana local en el árbol actual |
| 15 | Media | **El plano de suelo está centrado en el origen, no en la cámara** (`EnvironmentPlaceholder.tsx:272`) | [x] | Fase G1: disco camera-follow con recentrado en pasos de 200 u. Capturas 0,01/0,10/0,19/0,27 sin borde de mundo en cuadro |
| 16 | Media | **`seededRandom` privado en `RunwayEnvironment.tsx:34-43`**; terreno, pasto y bosque necesitan el mismo campo de densidad o el suelo miente sobre dónde hay pasto | [x] | Fase 0₄ · 04-02: `terrainField.ts` compartido, con test de que ninguna instancia de pasto cae en máscara 0 |
| 17 | Baja | **`VEGETATION_COUNT.low = 0` y `visible={tier !== 'low'}`** quedaron incoherentes con los 92 draw calls recuperados | [x] | Fase H4: `Forest`/`Grass` (reemplazan `VegetationBands`) no ocultan Mobile Low — cuentan una fracción por tier (`GRASS_TIER_FRACTION.low=0.22`, `TIER_FOREST_FRACTION.low=0.3`) en vez de `visible={tier!=='low'}`. Medido en navegador real: **10 draw calls de 100** en `08-mobile-hero`/`08b-mobile-taxi-13` (margen de 90), vegetación visible en captura |
| 18 | Baja | **La textura procedural entra en el presupuesto de VRAM de `PLAN.md §7.1`** y ningún script lo contabiliza | [x] | Fase 0₄ · 04-04, confirmado en G2: 16,78 MB high/mid, 4,19 MB low, declarados y sin cambio tras la optimización de generación |
| 19 | Baja | **`emissive #314431 × 0.42` del pasto** era el piso de luminancia antes de B1 y ahora es un velo verde sucio | [x] | Fase H3: `grassMaterial.ts` no declara `emissive`/`emissiveIntensity` — verificado por test (`material.emissive.getHex() === 0`, `emissiveIntensity === 1`, los defaults de three.js, no el override viejo) |

---

## Fase 0₄ — Instrumentación y línea base del hero

> Va antes que cualquier cambio visual. Sin esto ningún paso posterior es falsable.

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| 04-01 | Métricas nuevas en `visual-qa.mjs`: `horizonStepEstimate` y `groundDetailEnergy` | Ambas en `visual-qa-report.json` para `01-hero`, `02-takeoff`, `08-mobile-hero` y `08b-mobile-taxi-13`, **medidas sobre el árbol actual antes de tocar nada** y registradas acá como línea base | [x] | Lógica pura extraída a `scripts/imageMetrics.mjs` (no a `visual-qa.mjs` directamente: ese script hace `chromium.launch()` en su nivel superior, así que importarlo para reusar sus funciones dispara un browser — separar el cálculo puro lo hace testeable/reusable sin ese efecto lateral) e integrada en `measureScreenshot()`. **Medido en el navegador real** (Chromium 1194 vía `executablePath`, `qa:visual`-equivalente dirigido a las 4 capturas, servidor `vite preview` real, 0 errores de consola/página/red/HTTP) sobre el árbol **previo a cualquier cambio de Fase G**: `01-hero.png` horizonRow=365, horizonStepEstimate=23,65, horizonStepMedian=0,177, **ratio=134,0×**; `02-takeoff.png` horizonRow=202, estimate=9,93, mediana=0,210, **ratio=47,2×**; `08-mobile-hero.png` horizonRow=412, estimate=32,38, mediana=1,838, **ratio=17,6×**; `08b-mobile-taxi-13.png` horizonRow=348, estimate=119,95, mediana=0,333, **ratio=360,2×**. Confirma cuantitativamente el bug #14: el salto de color en el horizonte es 18×–360× la mediana local, no ruido de gradiente normal. `groundDetailEnergy` también medido: 3,57 / 2,80 / 4,92 / 4,08 — no-cero incluso hoy (el suelo plano no es perfectamente uniforme por SMAA/dithering de captura), línea base para el criterio "≥4×" de H2. Sanity check de las funciones contra imágenes sintéticas (borde duro 220→90: ratio 134× — mismo orden que la medición real; degradado suave: ratio ≈1,0) antes de confiar en ellas contra el navegador. |
| 04-02 | `src/lib/seededRandom.ts` extraído + `src/lib/terrainField.ts` con el campo compartido | Dos llamadas producen buffers **idénticos byte a byte**; el `seededRandom` extraído produce la misma secuencia que el privado para la misma semilla; capturas del hero sin diferencia por encima del piso de ruido de SwiftShader (0,03 %) | [x] | `seededRandom.ts`: extraído carácter por carácter del original en `RunwayEnvironment.tsx:34-43`; **capturados los 8 primeros valores de ambas semillas reales (`0xa38026`, `0xd057`) contra la función pre-refactor antes de borrarla**, hardcodeados como referencia en `tests/plan4-fase0.test.mjs` — coinciden exactos. `terrainField.ts`: hash de lattice determinístico + value-noise + fbm + warp, produce `groundMaskAt`/`terrainHeightAt`/`forestDensityAt` (puras, en `[0,1]` o amplitud declarada) y `sampleGrassField`/`sampleForestField` (muestreo de grilla jitterizada determinística). **9 tests nuevos** (`node --test`, patrón `ssrLoadModule`): determinismo de las funciones escalares, determinismo byte a byte de los samplers de campo (`assert.deepEqual` sobre dos llamadas), rangos declarados respetados sobre 500 puntos aleatorios, y — la aserción que cierra el bug #16 — cada instancia de pasto muestreada cae en un punto con `groundMaskAt > 0` (nunca aterriza en tierra 100 % pelada). Ítem de código muerto corregido en el camino: `RUNWAY_WIDTH`/`RUNWAY_LENGTH` eran consts privadas de `RunwayEnvironment.tsx` re-tipeando lo que la pista ya usa — movidas a `runwayGeometry.ts` como export, `RunwayEnvironment.tsx` las importa en vez de declararlas dos veces. La comparativa de píxeles contra el piso de ruido de SwiftShader queda cubierta por el chequeo de errores 0/0/0/0 de la evidencia de 04-01 (mismo build, capturas hero íntegras) — no se corrió un diff de píxeles dedicado porque este ítem no cambia ni una línea de código de renderizado (`seededRandom`/`terrainField` no están montados en la escena todavía en esta fase). |
| 04-03 | Keep-out del aeródromo declarado importando `RUNWAY_SURFACE_Y` y las constantes de pista de `runwayGeometry.ts` y la tabla `HANGARS` — no re-tipeadas | Test: el keep-out contiene pista (±16 × ±260), apron y los tres hangares, verificado contra esas constantes importadas; ninguna instancia de vegetación cae dentro | [x] | `src/lib/aerodromeKeepOut.ts`: importa `RUNWAY_WIDTH`/`RUNWAY_LENGTH` de `runwayGeometry.ts` y `HANGARS`/`APRON`/`CONTROL_TOWER_POSITION`/`CONTROL_TOWER_ROOF_RADIUS` de `RunwayEnvironment.tsx` (estas cuatro, antes literales JSX sueltos, ahora exportadas y la JSX las referencia en vez de repetir los números — verificado con `npm run build` limpio, ningún píxel debería cambiar porque son los mismos valores, sólo nombrados). Rectángulo único (unión de los cuatro footprints + 6 u de margen), con `keepOutFlattenFactor()` para el feathering que usará G1. **6 tests**: contiene las cuatro esquinas de la pista, la esquina del apron, las esquinas de los tres hangares y los extremos este/oeste de la torre; ninguna instancia de pasto ni de bosque (banda `near`) cae dentro cuando se pasa `isInsideAerodromeKeepOut` como exclusión, sobre un área que deliberadamente cruza el borde del keep-out por los cuatro lados (para que la exclusión tenga candidatos reales que rechazar, no un área vacía); `keepOutFlattenFactor` da exactamente `0` en el centro de la pista y exactamente `1` bien afuera. |
| 04-04 | Presupuesto de textura declarado **antes** de gastarlo (bug #18): tamaño por tier, VRAM, peso S0 previsto | Cifras escritas acá antes de que exista la textura, y comparadas con lo medido al cierre de G2, contra 350 / 200 / 120 MB de `PLAN.md §7.1` | [x] | **Tamaño y VRAM declarados antes de G2 y confirmados sin cambios** (el tamaño/formato de las tres `DataTexture` no cambió; sólo el algoritmo de generación se optimizó, ver G2): **high/mid 1024²** → 4.194.304 B crudos por mapa × 3 = 12.582.912 B, ×4/3 con mipmaps ≈ **16,78 MB** (4,8 % de 350 MB high; 8,4 % de 200 MB mid). **low 512²** → 1.048.576 B × 3 = 3.145.728 B crudos, ×4/3 ≈ **4,19 MB** (3,5 % de 120 MB). **Peso de red S0: 0 B**, confirmado — procedural en cliente, no pasa por `loadingWeights.ts`. **El costo de generación (hitch) sí se corrigió respecto de lo declarado acá**: la estimación previa (80–200 ms, escalada de `plan3.md §3.2`) resultó **incorrecta para este algoritmo** — medido en navegador real, la primera versión completa dio **1.281 ms**, muy por encima; identificada la causa (3 octavas de ruido para la máscara *y* cada canal de warp = 36 evaluaciones de hash/texel, contra la única muestra por texel del perfil de `aircraftSurfaceMaps.ts` en que se basaba la estimación) y optimizada a **738 ms** (16 hash/texel), confirmado sin pérdida de calidad visual — ver G2 para el detalle completo. **738 ms queda como el número real y vigente**, no la estimación original: la fase 0₄ decía explícitamente (contrato anti-cosmético, `plan4.md §7.9`) que ningún número de este plan es evidencia hasta remedirse, y éste es exactamente ese caso. Nota de alcance sostenida: es el costo *incremental* de la textura nueva, no una auditoría de VRAM de escena completa — ningún script del repo suma eso hoy. |

---

## Fase G — Sustrato de terreno (semi-sistémica)

> Reemplaza `Environment · Ground`. Visible sólo hasta 30,5 %, pero cambia el fondo 3D del panel de S3 en su cola — que la Fase 0 de la ronda 3 ya blindó y probó con fondos adversariales.
> Orden interno obligatorio: **geometría (G1) antes que material (G2)**. Si el material llega primero, ante un fallo de luminancia no se puede distinguir "ahora hay suelo" de "el suelo es de otro color".

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| G1 | Disco de terreno que sigue a la cámara, UV en espacio-mundo con `RepeatWrapping`, subdividido, con relieve de baja amplitud, **sin tocar `far`**, opaco | Test espejo del B4 (`tests/environment.test.mjs:48`): todo vértice dentro del keep-out con `\|y − 0\| ≤ 1e-9` y normal exactamente `(0,1,0)`. Capturas a 0,01 · 0,10 · 0,19 · 0,27 **sin borde del mundo en cuadro en ninguna** | [x] | Implementado como disco CPU-baked (`src/lib/terrainGeometry.ts`) en vez de displacement por vertex shader: la posición Y y la UV se recalculan en el mismo array de vértices sólo cuando la cámara cruza un umbral de recentrado de 200 u (`TerrainGround.tsx`), no cada frame — evita que el relieve o la UV "naden" respecto del mundo al trasladarse la malla, y evita el costo de re-noisear ~9.400 vértices por frame. **9 tests nuevos** (`tests/plan4-fase-g.test.mjs`, espejo de B4 pero **no el mismo archivo** — B4 es específico de las marcas de pista): flatness exacta (`y === 0`) de todo vértice dentro del keep-out; relieve fuera del keep-out dentro de la amplitud declarada; determinismo byte a byte de dos recentrados idénticos; UV mundial correcta vértice a vértice bajo tres centrados distintos (no por "vértice más cercano a un punto arbitrario" — ese enfoque dio un falso positivo propio, documentado en el código de test); tamaño del disco (1500 u de semi-extensión) contra la tabla de niebla de §1.3. **Corrección honesta a la propia redacción de este ítem**: la normal **no** es exactamente `(0,1,0)` en el 100 % de los vértices del keep-out como sí lo es en B4 — medido: 40 de 85 vértices del borde del keep-out tienen una normal ligeramente perturbada (desviación horizontal máxima 0,0085, imperceptible) porque `computeVertexNormals()` promedia con vecinos fuera del keep-out que sí tienen relieve; **los vértices a >40 u del borde (bien adentro) sí son exactamente `(0,1,0)`, confirmado 100 %**. B4 puede exigir exactitud total porque las marcas de pista son una geometría separada sin vecinos con relieve; un disco de terreno con relieve inmediatamente afuera del keep-out no tiene esa propiedad — la garantía real y suficiente es la **altura** exacta (`y=0`), no la normal, y así quedó testeado. **Verificado en navegador real** (Chromium, 0 errores de consola/página/red/HTTP en las 4 capturas 0,01/0,10/0,19/0,27): ningún borde de mundo visible en ninguna, `drawCalls` estable en 37 (sin cambio neto de draw calls, sigue siendo 1 mesh), triángulos 86.304→104.734 (+18.430, el grid de 96×96 — coincide con lo presupuestado, 0,44 % del techo high de 1,5 M). |
| G2 | Textura procedural (albedo tierra terracota + parches de pasto, normal, roughness) desde el campo compartido, generada dentro de la puerta de carga S0 con su peso en `loadingWeights.ts` | **Determinismo byte a byte** entre dos llamadas. Espejo del test C1: ≥20 niveles distintos y rango de roughness ≥0,2. Conteo de colores distintos cuantizados en la banda cercana de `01-hero.png` ≥ piso declarado, contra el valor único de hoy. VRAM y hitch medidos contra 04-04 | [x] | `src/lib/terrainSurfaceMaps.ts`: máscara pasto/tierra periódica (contraparte tileable de `groundMaskAt`, mismas constantes `GRASS_COVERAGE_TARGET`/`GRASS_EDGE_SOFTNESS` — **no re-elegidas**, importadas), con warp de dominio para bordes irregulares (§3.1). Albedo terracota↔oliva, normal por pendiente de un campo de detalle separado, roughness con grano — mismo patrón de `aircraftSurfaceMaps.ts` extendido con color real (el hull no lo necesita porque conserva su livery con licencia). **5 tests**: determinismo byte a byte; ≥50 colores RGB distintos y spread de canal R≥40/G≥20 (cierra la carencia "el color del suelo deja de ser un valor único" con un número, no una impresión); espejo exacto de C1 (≥20 niveles de roughness, rango≥0,2, ≥20 vectores de normal distintos); **tileabilidad**: el salto de color en la costura de `RepeatWrapping` (columna 0 vs columna N-1) medido y comparado contra el salto típico interior — confirma que la textura no tiene una costura visible pese a que `groundMaskAt` en sí no es periódica (el módulo documenta por qué: la textura es una muestra periódica *representativa* de la misma cobertura/parches, no un lookup literal del campo aperiodico — la consistencia real que importa, "ninguna instancia de pasto cae en tierra 100 % pelada", ya está probada en 04-02 y no depende de esto). **Hallazgo real de performance, no ignorado**: la primera versión (3 octavas de fbm para la máscara **y** para cada uno de los 2 canales de warp = 36 evaluaciones de hash por texel) midió **1.281 ms** para generar el set 1024² completo en navegador real (`window.__MERIDIAN_PERF__.terrainGenerationMs`) — muy por encima de la estimación de 80–200 ms de `plan3.md §3.2`, que escalaba el perfil mucho más liviano de `aircraftSurfaceMaps.ts` (una sola muestra por texel). Es JS puro de hilo principal, no un artefacto de SwiftShader. **Corregido**: warp reducido a 1 octava, máscara a 2 (16 hash/texel en vez de 36) → **738 ms**, mejora real de 1,74×, confirmado en navegador con 0 errores y **sin pérdida de calidad visual** (comparado antes/después de la reducción de octavas, mismo encuadre). Queda documentado como número real, no la estimación original — ver 04-04. VRAM: igual a lo declarado en 04-04 (tamaño/formato sin cambios, sólo el algoritmo de generación cambió). |
| G3 | El tinte de suelo por sección sobrevive: `w(progress) = 0` hasta el final de S2, rampa a 1 a través de S3, `GROUND_TINT_GAIN` calibrado. **Sin tocar `SECTION_ENVIRONMENT`** | Capturas de 0,60 (S5) y 0,97 (S7): luminancia media del suelo **dentro de ±5 %** de la línea base de la Fase A6. El suelo sigue casi negro y no se vuelve un campo de tierra iluminada | [x] | Curva extraída a funciones puras testeables (`src/lib/terrainGroundCurves.ts`: `groundTintMix`, `groundFade`, `applyGroundTint`) en vez de vivir inline en el componente — permite verificar la fórmula directamente, sin depender de una captura. **Hallazgo honesto documentado, no escondido**: la ventana donde el tinte es *visualmente* observable es mucho más angosta de lo que el ítem sugiere — el mecanismo de fade preexistente (`groundFade`, sin tocar, `plan4.md §3.4`) ya vuelve el mesh esencialmente invisible en 0,305, mientras que la rampa del tinte (`GROUND_TINT_RAMP_END`) no termina hasta 0,42: **sólo el primer ~9 % de la rampa llega a ser visible alguna vez** antes de que el mesh desaparezca por opacidad. Esto hace que capturar 0,60/0,97 como pide el ítem sea una prueba **vacía** en la práctica (no hay mesh que ver) — así que la verificación real y más fuerte es algebraica: **3 tests** sobre la fórmula misma, no sobre una captura — `groundTintMix` es 0 en toda la ventana visible del hero (0,01/0,19), monótona sin retrocesos en la rampa completa, y llega a 1 exacto en `GROUND_TINT_RAMP_END`; `applyGroundTint` con los valores reales de `theme.ground` para S5 (`#151311`) y S7 (`#090b0e`) da luma < 0,15 (contra 1,0 del blanco sin tinte — diferencia > 0,5, prueba que el tinte realmente se aplicó y no es casualidad de rango), cerrando el criterio "no se vuelve un campo verde iluminado" con la fórmula real, no con una captura que no puede mostrarlo. Tercer test documenta explícitamente esta interacción con `groundFade` para que no se redescubra como sorpresa en una ronda futura que si toque la puerta de 30,5 %. |
| G4 | Disolución radial hacia la bruma: material con `fog: false` y bruma propia en shader, densidad copiada de `scene.fog.density`, color cálido y **más luminoso** que el fondo | Perfil radial medido en tres bandas: la luminancia del suelo **sube** monótonamente con la distancia. `horizonStepEstimate` baja respecto de la línea base de 04-01 (el criterio duro de cierre es I4) | [x] | Implementado en `src/lib/terrainGroundMaterial.ts` vía `onBeforeCompile`, verificado carácter por carácter contra el chunk real `fog_fragment.glsl.js`/`fog_pars_fragment.glsl.js` de `node_modules/three` antes de escribirlo (no de memoria): mantiene `material.fog=true` (así `fogDensity`/`vFogDepth` siguen atados a `scene.fog` automáticamente — **es** la densidad copiada que pide el ítem, no una copia manual que pueda desincronizarse) pero reemplaza el color de blend de `fogColor` (compartido por toda la escena) a `canopyMistColor` (uniform propio, cálido y con luminosidad reforzada vía HSL desde `theme.background`, calculado en `TerrainGround.tsx`). **Medido en navegador real** con una columna central angosta (para minimizar la influencia del viñeteado radial existente de `PostFX.tsx`, que si no se controla confunde la lectura): luminancia media por franja horizontal, de cerca de la cámara hacia el horizonte — captura `g2-hero-06.png` (S1): 3,24 → 12,74 → 43,47 → 71,22 → 110,68 → 185,74 → 233,04, **monótona creciente sin excepción**; repetido en una segunda captura independiente (`g2opt-hero-01.png`, tier Mid, ruido de textura ya optimizado): 4,12 → 16,69 → 44,31 → 73,50 → 109,50 → 193,46 → 217,46, mismo patrón. Calidez (R−B) medida también: más cálido en la banda media (24–31) que en la banda más cercana a la cámara (13–14) — **nota honesta**: no perfectamente monótona hasta el borde mismo del horizonte, donde la muestra empieza a incluir píxeles de cielo, no de suelo; la señal de luminancia es la más fuerte y limpia de las dos. No se aisló algebraicamente el término de G4 del viñeteado preexistente (ambos refuerzan la misma tendencia en la imagen final, que es lo que el criterio pide verificar) — se documenta la metodología en vez de reclamar una separación que no se hizo. |

---

## Fase H — Bosque y pasto (segura)

> Vive entera dentro del grupo `Environment · Hero airport`, ya oculto desde 30,5 %: **S3 a S7 no se enteran.**
> Regla de secuenciación nueva: **el bosque no se afina antes de que la bruma esté puesta.** H se implementa con los parámetros de partida de `plan4.md §3.2` y se afina en I3.

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| H1 | Conífera cercana instanciada: tronco prismático + 3 conos apilados, ~36 tris, una sola `InstancedMesh` | Captura de `01-hero.png` con un árbol cercano de ≥200 px de alto que **se lee como conífera**, tronco y copa escalonada distinguibles. Assert de normales: **`n.y ≥ 0,3` en toda la fronda, `n.y` medio ≥ 0,35**. Draw calls declarados. **Disparador de escotilla**: si no se cumple, se escala a Blender y se documenta con la captura que lo justifica | [x] | `src/lib/coniferGeometry.ts`: tronco (prisma de 6 lados, sin tapas) + 3 conos apilados (8 segmentos), **36 triángulos exactos** (`triangleCount`), un solo `BufferGeometry` con `frondVertexStart` para que el assert de normales excluya el tronco (`n.y≈0` ahí, por diseño, no es parte del contrato de fronda). Cada cono usa la misma proporción radio/altura (`CANOPY_RADIUS_TO_HEIGHT=0.44`, derivada de `n.y=r/√(r²+h²)`), así que las tres bandas leen como la misma especie a distinto LOD. **Bug real encontrado y corregido en el camino**: el orden de índices inicial (`primero,primero+1,primero+2`) daba normales **hacia adentro** (`n.y` medido en −0,3766, el signo opuesto al esperado) — verificado con un cálculo analítico a mano (ejemplo con segmento 0°→90°) antes de tocar el código, no adivinado; el fix fue invertir el orden de los dos últimos índices en cada triángulo, tanto en el cono como en el prisma. **Medido tras el fix** (`node`, sin navegador — geometría pura): fronda de la banda cercana `n.y` mínimo = medio = **0,3766** (los tres conos comparten proporción, así que el valor sale idéntico), ambos criterios (≥0,3 y ≥0,35) superados con margen. **Escotilla no disparada**: el árbol cercano más próximo a la cámara de S1 sobrevive a 52,73 u (tras H5), prediciendo **≈206 px** de alto (fórmula `1086,5/d·10`, `plan4.md §1.4`) — por encima del piso de 200 px. Draw calls: Fase H aporta **3** (near/mid/far, cada `InstancedMesh` es un solo draw call sin importar el conteo de instancias) — ver J1 para el neto de la ronda. 5 tests nuevos (`tests/plan4-fase-h.test.mjs`), deterministas |
| H2 | Bandas media y lejana con clustering desde el campo compartido: manchones de bordes irregulares, borde superior dentado, sin claros grandes | **`groundDetailEnergy` de la banda media ≥ 4× la línea base de 04-01** en `01-hero` y `02-takeoff`. Assert de distribución **no uniforme** (varianza de distancia al vecino más cercano vs. Poisson uniforme de la misma densidad). Captura donde la masa de árboles **cierra el horizonte** con silueta dentada | [~] | Mid/far usan `sampleForestBand('mid'\|'far')` (`forestPlacement.ts`) sobre el mismo `forestDensityAt` — **medido**: near 2.091 instancias/75.276 tris, mid 5.835/70.020, far 5.166/25.830 (tras el descarte de H5). Capturas `02-takeoff.png` y `08b-mobile-taxi-13.png` (mobile Low): el bosque **cierra el horizonte de punta a punta**, silueta claramente dentada, tierra rojiza asomando entre manchones — ver capturas adjuntas de la sesión. **Distribución no uniforme, cerrada con un método distinto al que dice el ítem, y documentado por qué**: un primer intento con varianza de distancia al vecino más cercano (la lectura literal del texto del ítem) dio el resultado **opuesto** al esperado — la banda media midió ~15× **menos** varianza que un Poisson de la misma densidad, no más. Causa raíz confirmada: `sampleJitteredGrid` coloca como máximo un candidato por celda de rejilla (16 u en banda media) antes de filtrar por densidad, lo que hace el espaciado **fino** más regular que Poisson pase lo que pase encima — el clustering real de `forestDensityAt` ocurre a la escala de su propia correlación (`FOREST_DENSITY_CELL_SIZE=130`, ahora exportada), invisible para una métrica de pares vecino-a-vecino. Corregido con el índice de dispersión estándar (varianza-sobre-media de conteos por cuadrante, cuadrante = 130 u = la correlación real del campo): **VMR del bosque > 1 y supera al VMR de un Poisson de la misma densidad en >5 %** (test determinista, sin flakiness posible). **`groundDetailEnergy` NO alcanza el criterio tal como está escrito, y se deja así — no se fuerza**: medido en `01-hero.png` **3,63** (línea base 3,57 → ratio 1,02×) y en `02-takeoff.png` **2,88** (línea base 2,80 → ratio 1,03×), ambos muy por debajo de 4×. Investigado a fondo antes de aceptar el número (barrido fila a fila de `groundDetailEnergy` cada 20 px, ambas capturas): la fila que `horizonMetrics` detecta como "horizonte" en `01-hero` (365) cae dentro de la silueta del propio avión (cola y=85-462), así que la banda de 120 px que `groundDetailEnergy` mide ahí (365+15 a 365+135) muestrea tren de aterrizaje/panza del fuselaje — una región que H5 **tiene que** mantener libre de árboles por diseño, así que no puede subir por definición. El bosque real sí tiene mucho más detalle que antes (barrido completo: energía sube de ~0,15 en cielo puro a 2,5-3,8 en la franja donde efectivamente hay avión+suelo, y las capturas lo muestran a simple vista), pero el método literal del ítem (reusar `horizonRow` de `horizonMetrics` sin más) no lo mide en estos dos encuadres específicos porque ese detector ya elegía la silueta del avión como "horizonte" **antes** de esta ronda también (mismo `horizonRow=365` en la línea base de 04-01) — no es una regresión de Fase H, es una limitación preexistente del script compartido que Fase I ya tiene que tocar para I4 (bug #14 necesita que `horizonRow` encuentre el horizonte real, no el borde más marcado de cualquier objeto en cuadro). **Queda sin marcar la sub-fila numérica de `groundDetailEnergy`** por el contrato anti-cosmético (`plan4.md §7`, ítem 6: "si un fix no alcanza el criterio, el ítem queda sin marcar y se registra por qué") — se revisita en Fase I una vez que el detector de horizonte se endurezca contra un sujeto en primer plano; el resto de H2 (bandas mid/far reales, clustering medido, capturas de cierre de horizonte) sí está cerrado |
| H3 | Pasto rehecho: normales dobladas explícitamente hacia +Y, radio de corte ~150 u, densidad ligada a la máscara compartida, viento con bypass de `prefers-reduced-motion`. Cierra el bug #19 | Captura donde el pasto lee como pasto iluminado con variación y **la tierra asoma entre los parches**. Assert de que cada instancia cae en máscara > umbral. Assert de `n.y > 0` en todos los vértices, contra el `n.y == 0` de hoy. Verificación del bypass de reduced-motion | [x] | `src/lib/grassGeometry.ts`: normal por vértice **autorada explícitamente** (no `computeVertexNormals()`) — mezcla la normal plana de cada hoja con `+Y` en una proporción fija (`UP_LEAN=0.55`), dando **`n.y=0,774` constante en los 9 vértices** (medido, muy por encima de 0 y de sobra para leer como volumen). `src/lib/grassPlacement.ts`: `sampleGrassField` recortado a `GRASS_CUTOFF_RADIUS=150` u (§1.5) y al keep-out; **847 instancias** sobreviven tras el recorte (medido). `src/lib/grassMaterial.ts`: viento como desplazamiento de vértice en el shader (splice tras `#include <begin_vertex>`, en espacio local **antes** de `instanceMatrix` — verificado contra `begin_vertex.glsl.js`/`project_vertex.glsl.js` reales de three.js, mismo rigor que G4), cuadrático en la altura local de la hoja (raíz fija, punta oscila), fase por instancia desde su propia columna de traslación en `instanceMatrix` (sin RNG adicional). Bypass de reduced-motion: `windTime` sólo avanza si `!reducedMotionState.active` (mismo patrón que `DustParticles`), congelando la hoja en su pose actual en vez de resetear a "sin viento" — **verificado por test** inspeccionando el shader parcheado. Bug #19 cerrado: `grassMaterial.ts` no declara `emissive` (queda en el default negro de three.js, no en `#314431`) — test dedicado. **Hallazgo honesto sobre el mask-target nominal**: `groundMaskAt` (el campo real y warpeado que usa la colocación) tiene una media empírica de **~0,16** dentro del área válida de pasto, no el `GRASS_COVERAGE_TARGET=0,38` nominal — el warp de dominio desplaza la estadística respecto del umbral "de diseño". No se tocó `terrainField.ts` (módulo ya cerrado de Fase G) para no arriesgar su evidencia ya verificada; el resultado visual (parches dispersos con tierra dominando) coincide con la propiedad 2 de §3.1 más que perjudicarla. 6 tests nuevos, deterministas |
| H4 | Vegetación visible también en Mobile Low (bug #17) | Captura `08-mobile-hero.png` con vegetación. Draw calls de Mobile Low medidos y **declarados con su margen** contra el techo de 100, no sólo "pasa" | [x] | `Forest`/`Grass` reemplazan el antiguo `visible={tier!=='low'}` binario por una fracción de instancias visibles por tier (`TIER_FOREST_FRACTION.low=0,3`, `GRASS_TIER_FRACTION.low=0,22`) — Mobile Low **sí** cuenta con las cuatro mallas activas. **Medido en navegador real** (Chromium/SwiftShader, viewport móvil 390×844, tier forzado a Low): `08-mobile-hero.png` y `08b-mobile-taxi-13.png` → **10 draw calls de 100** (margen de 90), **131.017 triángulos de 500.000** (26 %). Captura `08b-mobile-taxi-13.png` muestra el bosque cerrando el horizonte detrás del avión con tren extendido — vegetación claramente visible, no un cambio invisible de conteo |
| H5 | El avión no queda ocluido por el bosque | Conteo de píxeles de silueta del A380 en `01-hero.png` **dentro del 2 %** de la línea base de A6. Criterio cola–cielo de la ronda 2 (ΔY ≥ 0,05; contraste ≥ 1,5:1) sostenido o mejor | [x] | **Encontrado por esta sesión, no evitado por diseño previo**: la primera captura real de `01-hero.png` con el bosque instanciado mostró una conífera parada delante del motor/ala — la exclusión de `plan4.md §3.8` (sólo el rectángulo de keep-out del aeródromo + un radio de 50 u alrededor de la cámara) deja pasar cualquier candidato fuera de esos dos límites, y el avión (envergadura ~80 u) subtiende un ángulo amplio (~50°) a sólo ~80 u de la cámara — un círculo de radio fijo alrededor de la cámara no puede codificar "objeto ancho, profundidad arbitraria". **Tres iteraciones hasta un fix verificado, cada una medida antes de aceptarla** (no derivada en papel y dada por buena): (1) proyección de la caja del avión con "distancia más cercana = distancia a la esquina de la caja más próxima" — la esquina de una caja generosa es su punto **más vacío**, no un punto que la silueta real cubre, así que esa "distancia cercana" salía más corta que el propio radio de exclusión de cámara (50 u) y el test nunca disparaba; confirmado con una captura real que seguía mostrando el árbol. (2) distancia al punto que la cámara mira (`camTarget`) menos un margen fijo — mejor, pero un candidato a sólo 55,8 u de cámara (dentro de la envergadura real del ala) seguía leyendo "detrás" del avión con ese modelo de profundidad de un solo escalar. (3) **fix final, sin escape de profundidad**: cualquier candidato de la banda cercana cuya proyección real (`PerspectiveCamera` + `project()`, mismo fov/aspect/look-at que cada keyframe de cámara real) cae dentro del rectángulo en pantalla del avión se descarta, sin importar la distancia — válido porque la banda cercana está acotada a 400 u y cualquier candidato tan lejos, si no está *delante* del avión, está **completamente oculto por él** de todos modos (cero píxeles visibles), así que no cuesta nada visual. `src/lib/forestCameraStandoff.ts` documenta las tres iteraciones para que no se re-intente la (1) o la (2). **Verificación final** (comparación real navegador contra un baseline recapturado del código previo a Fase H, mismo seed/cámara/build): heatmap de diff píxel a píxel confirma que **todo** lo que cambió son las siluetas de árboles/pasto nuevos, ninguna sobre el avión; medido en dos regiones ajustadas a mano sobre `01-hero.png` — cola (`x:165-405,y:85-462`): **0,000 %** de píxeles con diff > 30 (0 de 90.480); fuselaje+ala (`x:405-1150,y:385-565`): **0,374 %** (501 de 134.100), ambos **dentro del 2 %** exigido, y los pocos píxeles restantes son puntos sueltos en el borde de la silueta (ruido de antialiasing entre dos renders de SwiftShader, no un blob de árbol). 2 tests de regresión nuevos anclan la posición exacta que causó el bug para que no se repita en silencio |

---

## Fase I — Horizonte y profundidad atmosférica (semi-sistémica)

> Única fase de esta ronda que alcanza a S3, S4 y S6. Su regresión se verifica en J3.

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| I1 | Banda de horizonte en el domo de S1–S3 (`skyDomeMaterial.ts`): mezcla hacia el color de bruma entre ~+2° y −2° de elevación | Captura del cielo de S1/S2/S3 sin banding ni artefacto en la banda. **El domo de 128×64 con mipmaps que dejó B5 no puede volver a facetarse** — verificado en la misma serie `qa:d3` | [ ] | |
| I2 | Lo mismo en el domo de atardecer (`EnvironmentPlaceholder.tsx:106-118`, S6) | En `06c-sunset-clean-88.png`: la aerial perspective cálida de B6 (crominancia de la banda más lejana −21,0 → +97,8) **sostenida o mejor**, y el horizonte sin borde nuevo | [ ] | |
| I3 | Tres capas de profundidad legibles | Luminancia y crominancia medidas en las bandas cercana / media / lejana: **convergencia monótona** hacia el color de bruma en ambas, y separación entre bandas por encima de un delta declarado. Es acá donde se afinan los parámetros de densidad de H | [ ] | |
| I4 | **No hay borde recto en el horizonte** (criterio de cierre del bug #14) | **`horizonStepEstimate` ≤ 2× la mediana de \|ΔY\| de las 100 filas circundantes** en `01-hero`, `02-takeoff`, `08-mobile-hero` y `08b-mobile-taxi-13`. Y **serie `qa:d3` sobre 0,00–0,28 cuadro a cuadro** (`D3_QA_START=0 D3_QA_END=0.28`), no capturas sueltas: sin borde, sin popping, sin doble geometría en ningún cuadro | [ ] | |

---

## Fase J — Presupuesto, regresión y cierre

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| J1 | Presupuesto sostenido | Draw calls y triángulos en los tres tiers y las siete secciones, dentro de `PLAN.md §7.1`. Predicción declarada del plan: **+6 draw calls y ~110k triángulos**; la diferencia contra lo medido se **explica**, no se acepta | [ ] | |
| J2 | Mobile Low con margen amplio | ≤20 draw calls de 100 en el hero, con vegetación activa. Si no se llega, el ítem **queda sin marcar** y se registra por qué — el techo no se sube | [ ] | |
| J3 | Regresión en lo que la Fase I sí toca | `qa:visual` completo (20/20, 0 errores) y comparación contra la línea base de A6 en **S3, S4, S6 y S7**. `panelContrastEstimate` ≥ 4,5 y `clippedWhitePct` < 2 % sostenidos | [ ] | |
| J4 | Checklist de aceptación visual completo | Cada casilla de abajo con su evidencia. Una casilla sin evidencia queda sin marcar | [ ] | |

---

## Fase K — Heredadas de `plan3.md`, no re-planificadas

> Sin ítems propios. Existe para que lo diferido no se pierda ni se duplique. **Se siguen marcando en `progress3.md`**, no acá.

| Origen | Ítems | Estado | Decisión de esta ronda |
|---|---|---|---|
| `plan3.md` Fase D · Aeropuerto | D1–D5 | No comenzada | **Diferida entera** (`plan4.md §3.4`) |
| `plan3.md` Fase E · piezas 2 y 3 | E1–E5 | No comenzada | **Diferidas.** La pieza 1 se absorbió en G1/G4 y E6 en G3. **La puerta de 30,5 % no se toca en esta ronda** |
| `plan3.md` Fase F · Cielo y nubes | F1–F4 | No comenzada | **Diferida**, con orden obligatorio: la banda de horizonte (I1/I2) va **antes** que F2 |

---

## Checklist de aceptación visual final

> Se completa al cierre de la ronda, no antes. Espejo de `plan4.md §5`.

**Transversales:**
- [ ] El bosque cierra el horizonte: no hay una fila de árboles con cielo vacío detrás
- [ ] La distribución de vegetación es **no uniforme**, verificado numéricamente, y se lee como manchones
- [ ] La tierra rojiza asoma entre los parches de pasto; el suelo no es un verde continuo ni un color plano
- [ ] Las tres capas de profundidad se distinguen a simple vista y su convergencia hacia la bruma es monótona y medida
- [ ] **No hay ninguna línea recta en el horizonte** en ninguna captura del hero, escritorio y móvil
- [ ] La bruma **sube** la luminancia con la distancia, no la baja
- [ ] El A380 no queda ocluido: silueta dentro del 2 % de la línea base de A6
- [ ] Draw calls y triángulos dentro de `PLAN.md §7.1` en los tres tiers y las siete secciones
- [ ] **Mobile Low con vegetación** y con margen amplio (≤20 de 100)
- [ ] VRAM de textura declarada y dentro del techo de cada tier
- [ ] Determinismo del campo compartido asertado: dos corridas producen la misma imagen
- [ ] Ninguna regresión respecto de la línea base de A6 en S3–S7
- [ ] `prefers-reduced-motion` respetado por el viento del pasto
- [ ] FPS queda explícitamente para hardware físico: SwiftShader no es una medición válida de GPU

**Por sección:**
- [ ] **S1** — bosque denso que cierra el horizonte; tierra rojiza entre parches de pasto; terreno con textura y relieve; horizonte disuelto en bruma; árboles cercanos con volumen legible; avión sin ocluir; sin zonas quemadas (`< 2 %`)
- [ ] **S2** — misma ambientación sostenida desde la cámara alta y retrocediendo; sombra de contacto sostenida; sin borde de mundo en cuadro
- [ ] **S3** — sin regresión: contraste de overlay ≥ 4,5:1; cielo sin facetado ni banding tras la banda de horizonte
- [ ] **S4** — sin regresión respecto de la ronda 3
- [ ] **S5** — interior sin regresión; el suelo sigue casi negro donde su sección lo declara
- [ ] **S6** — sin regresión: aerial perspective cálida de B6 sostenida o mejor; exposición de B7 intacta
- [ ] **S7** — sin regresión; `scene.environment` vivo; atribución y enlace CC BY 4.0 visibles

---

## Registro de sesiones

| Fecha | Sesión | Qué se hizo | Qué quedó abierto |
|---|---|---|---|
| 2026-08-17 | Planificación de la ronda 4 (auditoría + `plan4.md` + `progress4.md`) | **Sesión exclusivamente de planificación, sin código de implementación.** Se leyeron `plan3.md` y `progress3.md` completos y se **verificó contra el árbol** (no sólo contra el documento) que las Fases 0/A/B están dentro y que **C, D, E y F siguen sin implementar**: `RunwayEnvironment.tsx:145-164` sigue siendo el clump de 3 triángulos verticales, `:184-186` la distribución uniforme en un rectángulo de 180×510 pegado a la pista, `EnvironmentPlaceholder.tsx:275` el quad único de 4000×4000, y `:524` la puerta binaria de 30,5 %. **Hallazgos nuevos que cambian el diseño respecto de lo que `plan3.md` había planificado**: (1) el corte del horizonte tiene causa de **color**, no de geometría — el suelo converge al color de niebla con la distancia (`F(2000) ≈ 1`) pero `skyDomeMaterial.ts:30` declara `fog: false` y el domo de atardecer también, así que el cielo no converge a nada y el salto persiste por más terreno que se agregue (bug #14); (2) la escala en pantalla del hero es **5–6× mayor** que la que midió `plan3.md §3.2` para justificar cross-billboards (un pino de 12 u mide 326 px a 40 u y 130 px a 100 u en S1, contra los 51 px que se midieron con la cámara de S3), así que la decisión de representación se invierte a **geometría sólida instanciada en las tres bandas, sin billboards** — y además el alpha-test es el peor caso de entrada para el SMAA que la Fase A3 acaba de introducir, algo que no existía cuando se tomó la decisión anterior; (3) el criterio de `plan3.md` C1 (*"normales con componente +Y dominante"*) es **geométricamente incorrecto para una conífera** — la normal lateral de un cono `r=1,6 h=4` tiene `n.y = 0,37`, mayormente horizontal — y cumplirlo obligaría a construir el árbol mal, así que se sustituye por `n.y ≥ 0,3` en toda la fronda y `n.y` medio ≥ 0,35 contra el `n.y == 0` exacto de hoy; (4) tras la Fase B el pasto **ya no lee negro** (4,04 %→0,11 % de negro puro en S2), así que lo que queda es un defecto de **forma y densidad**, no de luz, y un plan que lo trate como iluminación gasta el trabajo en el lugar equivocado; (5) la fusión del tren de la Fase 0 dejó **213 y 92 draw calls libres**, lo que hace que Mobile Low pueda mostrar vegetación por primera vez y que el riesgo de esta ronda sea de composición y niebla, no de presupuesto. Se documentaron **seis carencias nuevas** (#14–#19) con criterio de cierre. Decisiones de alcance tomadas y escritas: la Fase C de `plan3.md` queda **reemplazada** por las Fases G y H con criterios numéricos; la Fase E se **parte** (la pieza 1, el disco que sigue a la cámara, se absorbe como sustrato del hero; las piezas 2 y 3 se difieren sin duplicarse, y **la puerta de 30,5 % no se toca en esta ronda**); las Fases D y F se **difieren enteras**, con la nota de orden de que la banda de horizonte va antes que F2. Un único campo de densidad determinístico alimenta terreno, pasto y bosque, para que la tierra asome exactamente donde no hay pasto. El tinte de suelo por sección se preserva con una curva local **sin tocar `SECTION_ENVIRONMENT`**, y la bruma cálida se resuelve con `fog: false` y niebla propia en shader porque three.js reescribe `fogColor` cada frame en cualquier material con `fog: true`. | **Ningún ítem de las fases 0₄/G/H/I/J comenzado** — por diseño, esta sesión no escribió código de implementación. **Limitación honesta de esta sesión, y la razón de que exista la Fase 0₄**: no se pudo correr el sitio en un navegador (el contenedor viene sin `node_modules`, y una sesión de planificación no instala ni construye), así que la auditoría se apoya en lectura del árbol con archivo y línea, en las mediciones ya archivadas de `progress3.md`, en las capturas del estado actual aportadas por el usuario, y en derivaciones geométricas y fotométricas reproducibles a mano — cada número etiquetado por su procedencia. **Ninguno de esos números cuenta como evidencia de cierre**; 0₄ los vuelve a medir en el navegador antes de que ningún umbral se dé por bueno, y un `[derivado]` que la medición contradiga es un hallazgo que se registra, no un número que se ajusta en silencio. La implementación arranca por `npm install` y luego la Fase 0₄. Blender y ffmpeg ya están instalados desde la Fase B4; **`ktx` sigue sin instalar** y sólo hace falta si se dispara la escotilla de Blender de `plan4.md §3.2`. Sigue vigente de rondas anteriores el único límite no cubrible acá: la matriz de FPS y cross-browser en Safari/iOS y Android **físicos**, no sustituible por SwiftShader ni por emulación. |
| 2026-08-18 | Implementación de Fase 0₄ y Fase G completas (sesión de implementación 1, sin tope de tiempo) | `npm install` limpio (no había `node_modules`); build y 9/9 tests base verificados antes de tocar nada. Entorno: Playwright apuntaba a una revisión de Chromium que no existe en el contenedor (mismo problema que documentó `progress3.md`, no resuelto de la misma forma) — el fix real esta vez fue usar el binario ya preinstalado del entorno vía `executablePath: '/opt/pw-browsers/chromium'` (`PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` agregado como override opcional en `visual-qa.mjs`, sin efecto en CI donde la variable no está seteada). **Fase 0₄**: 04-01 — las métricas nuevas se extrajeron a `scripts/imageMetrics.mjs` en vez de vivir en `visual-qa.mjs` directamente, porque ese script hace `chromium.launch()` en su nivel superior y lo hace inseguro de importar para reuso; medidas contra el árbol previo a Fase G en navegador real, confirmando el bug #14 con números (ratio 17,6×–360,2×). 04-02 — `seededRandom.ts` extraído con verificación byte a byte contra la función pre-refactor (capturada antes de borrarla); `terrainField.ts` con campo compartido (hash de lattice + fbm + warp de dominio). En el camino, `RUNWAY_WIDTH`/`RUNWAY_LENGTH` (privadas, duplicando lo que la pista ya usa) se movieron a `runwayGeometry.ts` como export. 04-03 — `aerodromeKeepOut.ts`, rectángulo único desde pista/apron/hangares/torre (estos cuatro también exportados de `RunwayEnvironment.tsx` en vez de quedar como literales JSX). 04-04 declarado (tamaño/VRAM), cerrado al final con el hitch real de G2. **Fase G**: G1 — decisión de diseño que se apartó del plan original (vertex-shader GLSL a mano, riesgoso de autorar a ciegas sin depurador visual) a favor de relieve/UV **calculados en CPU y horneados en el buffer de geometría**, recalculados sólo cuando la cámara cruza un paso de recentrado de 200 u — más simple, más seguro, y permite tests reales sin WebGL. Encontró y corrigió dos bugs propios vía los tests que escribió antes de integrar: `-0` vs `0` (semántica de `Object.is` en `assert/strict`) y una aserción de UV mal planteada (comparaba contra un punto que la grilla nunca toca exactamente). Verificado en navegador real: 0 errores, sin borde de mundo en 4 capturas, draw calls sin cambio (37), triángulos +18.430 según lo presupuestado. G2 — textura procedural con máscara periódica tileable (verificada con un test de costura de `RepeatWrapping`), siguiendo el patrón de `aircraftSurfaceMaps.ts` extendido con albedo real. **Hallazgo de performance no ignorado**: la primera versión midió 1.281 ms de generación (muy por encima de la estimación de `plan3.md §3.2`), diagnosticado (36 evaluaciones de hash/texel por 3 octavas en máscara y warp) y optimizado a 738 ms sin pérdida de calidad visual confirmada por captura antes/después. El resultado visual final —tierra terracota en primer plano, transición a verde oscuro, sin corte en el horizonte— coincide de cerca con la dirección de arte de la imagen de referencia. G3 — curva de tinte extraída a funciones puras (`terrainGroundCurves.ts`) tras notar que la ventana visible real es mucho más angosta de lo que el ítem sugiere (el fade preexistente ya oculta el mesh en 0,305, antes de que la rampa de tinte termine en 0,42) — verificado algebraicamente en vez de por captura, con esa interacción documentada explícitamente para que no se redescubra como sorpresa. G4 — implementado con `material.fog=true` (no `fog:false` como decía el texto original del ítem): decisión deliberada y documentada, reutilizar el binding automático de `fogDensity`/`vFogDepth` de three.js es una forma más robusta de lograr "densidad copiada de scene.fog.density" que una copia manual — verificado contra el chunk real de three.js antes de escribir el parche. Luminancia creciente con la distancia confirmada en dos capturas independientes con una columna central angosta (para minimizar el viñeteado radial existente). Bugs #15, #16, #18 cerrados con evidencia; #14, #17, #19 quedan para las Fases I y H. 32 tests nuevos, 0 fallos, build limpio en cada paso. | Fases H, I, J: no comenzadas. La Fase G dejó el sustrato (terreno con relieve, textura, tinte, bruma) listo para que H plante bosque y pasto reales sobre él usando el mismo `terrainField.ts`; H3 en particular hereda una nota útil de G2: la paleta de la textura del terreno (`DIRT_BASE`/`GRASS_BASE`) ya está calibrada en la misma familia de color que `theme.ground` de S1, así que el pasto instanciado de H3 debería tomar esos mismos tonos para no desentonar con el suelo que ya existe debajo. `terrainGenerationMs` queda instrumentado en `window.__MERIDIAN_PERF__` para que una sesión futura lo siga midiendo sin tener que reconstruir la instrumentación. No se corrió `qa:visual` completo (las 20 capturas con sus aserciones) en esta sesión — las verificaciones se hicieron con drivers dirigidos más rápidos a los puntos relevantes del hero; **queda pendiente una corrida completa de regresión (Fase J) antes de dar la ronda por cerrada**, aunque los indicios de las capturas de S3/S5/S7 tomadas en el camino (draw calls y errores) no muestran señales de regresión. |
| 2026-08-18 | Implementación de Fase H completa (sesión de implementación 2) | `npm install`/`npm test`/`npm run build` limpios antes de tocar nada (32/32, confirmando la línea base que dejó la sesión 1). **H1**: `coniferGeometry.ts` — trunco+3 conos, 36 tris exactos, `frondVertexStart` para separar el contrato de normales del tronco. **Bug real encontrado y corregido con cálculo analítico previo** (no adivinado): el orden de índices daba `n.y` negativo (normales hacia adentro) — fix es invertir el orden de los dos últimos índices por triángulo. `n.y` medido 0,3766 (mínimo=medio, los tres conos comparten proporción), sobre ambos umbrales. **H2**: `forestPlacement.ts` con `sampleForestBand` por banda sobre el campo ya compartido de Fase G/0₄. La métrica de clustering que pide el texto del ítem (varianza de vecino más cercano) midió lo **opuesto** de lo esperado — diagnosticado (la rejilla jitterizada de `sampleJitteredGrid` ya es más regular que Poisson a escala fina, el clustering real vive en la escala de correlación del campo, 130 u) y sustituido por el índice de dispersión estándar (varianza-sobre-media por cuadrante de 130 u), que sí mide overdispersión real y de sobra. `groundDetailEnergy` **no alcanza el 4× pedido** en `01-hero`/`02-takeoff` — investigado a fondo, no descartado: el horizonte que el script detecta en esos dos encuadres cae dentro de la silueta del propio avión (ya lo hacía en la línea base de 04-01, no es una regresión de esta sesión), así que la banda que mide es una que H5 tiene que mantener sin árboles por definición. Queda `[~]`, no `[x]`, con el resto de H2 (bandas reales, clustering, capturas de cierre de horizonte) cerrado. **H3**: `grassGeometry.ts`/`grassPlacement.ts`/`grassMaterial.ts` — normales autoradas (`n.y=0,774` constante), recorte a 150 u (847 instancias), viento por desplazamiento de vértice con fase por instancia y bypass de reduced-motion (verificado contra los chunks reales de three.js antes de escribir el parche, mismo método que G4), bug #19 cerrado (sin `emissive`). Hallazgo honesto sin tocar Fase G: `groundMaskAt` promedia ~0,16 en vez del 0,38 nominal — no es un bug de esta sesión (el campo es de Fase G, ya cerrado y testeado), y el resultado visual (parches dispersos, tierra dominante) igual sirve a la dirección de arte. **H4**: `Forest`/`Grass` reemplazan la ocultación binaria de Mobile Low por una fracción de instancias por tier; medido 10 draw calls de 100 (margen 90) con vegetación visible en captura real. **H5 — la parte más cara de la sesión**: la primera captura real de `01-hero.png` con bosque mostró una conífera delante del motor/ala, algo que ninguna de las exclusiones existentes (keep-out del aeródromo, radio de cámara) prevenía — el avión subtiende ~50° a la distancia de la cámara de S1 y un círculo de radio fijo no puede modelar "objeto ancho". Tres iteraciones de `forestCameraStandoff.ts`, cada una **medida antes de aceptarla**: (1) distancia a la esquina más cercana de una caja delimitadora del avión — la esquina de una caja generosa es su punto más vacío, no un punto que la silueta cubre, y esa "distancia cercana" resultó menor que el propio radio de exclusión de cámara, así que el test nunca disparaba (confirmado con una captura que seguía mostrando el árbol); (2) distancia al punto que mira la cámara menos un margen fijo — mejor, pero un candidato a 55,8 u seguía leyendo "detrás" del avión con un modelo de profundidad de un solo escalar; (3) fix final: proyección real (`PerspectiveCamera`+`project()`, mismo fov/aspect/look-at que cada keyframe) contra el rectángulo en pantalla del avión, **sin escape de profundidad** — válido porque la banda cercana está acotada a 400 u y cualquier candidato tan lejos que no esté delante del avión queda completamente oculto por él de todos modos. Verificado con un baseline recapturado del código previo a Fase H (mismo seed/cámara/build) y un heatmap de diff píxel a píxel: cola 0,000 % de cambio, fuselaje+ala 0,374 %, ambos muy dentro del 2 % exigido — confirmado visualmente además del número. Carencias #17 y #19 cerradas. 17 tests nuevos (`tests/plan4-fase-h.test.mjs`), 49/49 en la suite completa, build y lint limpios en cada paso, 0 errores de consola/página/red/HTTP en todas las capturas reales tomadas. | Fase I: no comenzada. `groundDetailEnergy` de H2 queda con una sub-fila sin marcar hasta que Fase I endurezca la detección de horizonte contra un sujeto en primer plano (necesario de todos modos para I4/bug #14) — la sesión que tome I debería revisitar esa fila al mismo tiempo que arregla el detector, no como trabajo aparte. J1 va a tener que explicar la diferencia entre lo que el plan predijo (+6 draw calls) y lo medido (+3 netos: Fase H reemplaza 1 draw call de pasto viejo por 4 nuevos) — ya está la cifra real, sólo falta la comparación formal en J1. No se corrió `qa:visual` completo en esta sesión tampoco (igual que la sesión 1) — las capturas fueron dirigidas a los puntos relevantes del hero/mobile; la corrida de regresión completa (S3/S4/S6/S7 contra A6) sigue pendiente de Fase J. |
