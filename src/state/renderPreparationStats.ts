/** Wall-clock preparation timings captured once for runtime QA. */
export const renderPreparationStats: {
  textureUploadMs: number | null
  shaderCompileWaitMs: number | null
} = {
  textureUploadMs: null,
  shaderCompileWaitMs: null,
}
