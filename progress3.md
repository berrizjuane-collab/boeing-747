# PROGRESS 3 — Fidelidad de Entorno y Dirección de Arte

> Checklist espejo de [`plan3.md`](./plan3.md). **Documento activo de tracking.** `PROGRESS.md` (ronda 1) y `progress2.md` (ronda 2) quedan cerrados y no se vuelven a marcar.
>
> **Convención de estados:** `[ ]` pendiente · `[~]` en curso / condicionado · `[x]` completo · `[!]` bloqueado
>
> **Regla de marcado (contrato anti-cosmético, `plan3.md §7`):** ningún ítem se marca sin evidencia adjunta en su propia fila — captura a un % de scroll específico, medición numérica, o assert automatizado. "Debería estar arreglado" no es evidencia. Si un fix no alcanza el criterio, el ítem **queda sin marcar** y se registra por qué, en vez de rebajar el criterio.

---

## Estado global

**Ronda 3 completa: Fases 0, A, B, C, D, E y F, las 71 casillas con evidencia adjunta.** Fase 0: instrumentación y presupuesto. Fase A (sistémica): base de imagen (tone mapping, grading, antialiasing, `scene.environment`). Fase B (sistémica): luz y atmósfera, incluyendo Blender y las tres HDRI regeneradas. Fase C: terreno y naturaleza (árboles, pasto, terreno procedural, línea de horizonte). Fase D: aeropuerto (hangares, torre, pista con desgaste y numeración, luces, calles de rodaje). Fase E: el mundo persiste en altura — la pista se ve desde arriba, sin corte duro de scroll, con un disco de terreno que se disuelve en niebla. Fase F: cielo y nubes (sombreado real, nubes en S1/S2, disco solar y god rays en S6). **Los 13 bugs de la auditoría original están cerrados con evidencia**, más varios bugs adicionales encontrados y corregidos durante la implementación de C–F (ver el registro de sesiones del 2026-08-16/17). El checklist de aceptación visual final (transversales + las siete secciones) también está cerrado.

---

## Línea base medida (referencia contra la que se compara todo)

Medición propia sobre el sitio corriendo, leyendo `window.__MERIDIAN_PERF__`:

| scroll | 0.00 | 0.16 | 0.22 | 0.26 | 0.30 | 0.84 | 0.90 |
|---|---|---|---|---|---|---|---|
| draw calls | 148 | 148 | 148 | 34 | 34 | 21 | 20 |
| triángulos | 71.080 | 71.080 | 71.080 | 45.140 | 45.140 | 44.104 | 43.624 |

Desglose del hero por frustum real:

| viewport | casco `A380` | `LandingGear_Part_*` | total exterior | no-exterior | total |
|---|---|---|---|---|---|
| 390×844 (Mobile Low) | 1 | 92 | 93 | 6 | **99** / 100 |
| 1440×900 (Desktop High) | 1 | 115 | 116 | 32 | **148** / 250 |

Luminancia de las tres HDRI (`npm run qa:hdri`):

| archivo | resolución | media | **mediana** | p95 | máx | bytes |
|---|---|---|---|---|---|---|
| `golden-hour.hdr` | 2048×1024 | 1,00358 | **0,23368** | 1,35362 | 202.427 | 1.392.889 |
| `high-altitude.hdr` | 2048×1024 | 1,25455 | **0,07051** | 0,31445 | 142.863 | 400.475 |
| `sunset.hdr` | 2048×1024 | 0,65274 | **0,11566** | 0,79877 | 142.415 | 1.191.614 |

Ratio de medias **1,922×** (pasa `<4×`). Ratio de medianas **3,31×** — no asertado hoy, ver bug #3.

Payload bloqueante S0 real: **5,20 MB** de 15 MB.

**La tabla de arriba es la línea base de la sesión de planificación (previa a toda implementación).** Tras Fase 0 (fusión de `LandingGear`) y Fase A (antialiasing agregado), los números vigentes — medidos con `qa:visual` completo, 0 errores — son:

| | hero desktop-high (0.01) | hero mobile-low (0.01) |
|---|---|---|
| draw calls, línea base (planificación) | 148 | 99 |
| draw calls, tras Fase 0 (merge de tren) | 34 | 8 |
| draw calls, tras Fase A (+ SMAA) | **37** | **8** |
| draw calls, tras Fase B (sin cambio — B1/B2/B6/B7 son luz/atmósfera, no geometría) | 37 | 8 |
| triángulos, tras Fase A | 72.160 | 69.698 |
| triángulos, tras Fase B (+ domo de cielo 128×64, B5) | 86.304 | 83.842 |

Mobile Low se mantiene exactamente en el objetivo de Fase 0 (≤10) — SMAA se excluyó deliberadamente de ese tier, ver Fase A · A3. Desktop High queda en 37, por encima del `≤36` que Fase 0 había fijado antes de que existiera antialiasing en el presupuesto, pero con margen amplísimo contra el techo real (`<250`). El salto de triángulos de Fase B es enteramente el domo de cielo (B5); +14.144 en desktop-high es 0,94 % del techo de ese tier (1.500.000).

---

## Bugs reales encontrados en la auditoría

Ninguno reportado por el usuario; todos salieron de leer el código y medir. Detalle y criterio de cierre en `plan3.md §2`.

| # | Sev. | Bug | Estado | Evidencia |
|---|---|---|---|---|
| 1 | Alta | Tone mapping es **AgX, no ACES** — `<ToneMapping/>` sin prop `mode` | [x] | Fase A · A1 |
| 2 | Alta | `scene.environment` queda **nulo desde S7** | [x] | Fase A · A4 |
| 3 | Alta | Calibración de HDRI **dominada por el disco solar**; medianas descalibradas 3,31× | [x] | Fase B3 |
| 4 | Media | Grading pre-tone-mapping con umbrales LDR sobre buffer HDR | [x] | Fase A · A2 |
| 5 | Media | `SectionGradeEffect.strength` congelado en `0.5` | [x] | Fase A · A2 |
| 6 | Media | **Sin antialiasing de ningún tipo** | [x] | Fase A · A3 |
| 7 | Baja | `castShadow`/`receiveShadow` del exterior es código muerto | [x] | Fase A · A5 |
| 8 | Baja | `gl.toneMapping` / `gl.toneMappingExposure` son no-ops que engañan | [x] | Fase A · A5 |
| 9 | Baja | `skyDomeMaterial.ts` sin includes de tonemapping/colorspace | [x] | Fase A · A5 |
| 10 | Baja | `qualityStore.ts:22` referencia `SHADOW_SECTION_END`, inexistente | [x] | Fase A · A5 |
| 11 | Baja | `VEGETATION_COUNT.low = 144` es valor muerto | [x] | Fase A · A5 |
| 12 | Alta | **El panel de S3 no tiene tarjeta de fondo** — es la aserción de CI que se rompe primero | [x] | Fase 0 · ítem 03 |
| 13 | Baja | `loadingWeights.ts` con pesos de HDRI obsoletos (high-altitude +43 %) | [x] | Fase 0 · ítem 04 corrigió los pesos contra el disco; revalidados tras Fase B4 (que regeneró los tres archivos de HDRI a 4096×2048) — pesos actualizados de nuevo contra los bytes reales post-B4 |

---

## Fase 0 — Instrumentación y presupuesto

> Va antes que cualquier cambio visual. Sin esto ningún paso posterior es falsable.

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| 01 | Sondas móviles nuevas en `visual-qa.mjs` a **0,13 · 0,24 · 0,30** | El reporte incluye las tres capturas con su `performance`, y el pico real de draw calls de S1/S2 en Mobile Low queda medido | [x] | `visual-qa.mjs` captura `08b-mobile-taxi-13.png`(0.13)/`08c-mobile-gear-retract-24.png`(0.24)/`08d-mobile-terrain-30.png`(0.30) con `performance` cada una. Medido contra el árbol pre-merge (0 errores): drawCalls 99/99/7/8 en 0.01/0.13/0.24/0.30 — **el pico de 99 se sostiene hasta 13 % y cae a 7–8 ya en 24 %**, antes de lo que el nombre de la sonda sugería (el tren sale del frustum de Mobile Low antes de terminar de replegarse). Post-merge (ítem 02): ver fila 02. |
| 02 | **Fusión de las 115 mallas de `LandingGear` en runtime** | Mobile Low hero **99 → ≤10** y Desktop High hero **148 → ≤36**, medidos. Y `01-hero.png` / `08-mobile-hero.png` **idénticos píxel a píxel** a los artefactos de la ronda 2 — cualquier diferencia es un bug | [x] | Implementado en `ExteriorAsset.tsx` (merge relativo a `gear` vía cadena completa de `matrixWorld`, con `three/examples/jsm/utils/BufferGeometryUtils.js#mergeGeometries`). **Trampa real encontrada y evitada**: `mergeBufferGeometries` de `three-stdlib` está roto — sus checks de consistencia hacen `return null` dentro de un callback `forEach`, que sólo corta esa iteración, no la función — y produjo un merge corrupto silencioso (posición con conteo de vértices no entero) que rompió `computeBoundingSphere` con NaN y perdió el contexto WebGL; cambiado al `mergeGeometries` mantenido de `three/examples/jsm`. **Draw calls, medidos con `qa:visual` completo (0 errores de consola/página/red/HTTP en cuatro corridas)**: Desktop High hero **148 → 34** (mejor que el objetivo ≤36); Mobile Low hero **99 → 8** (mejor que el objetivo ≤10); sondas nuevas de la fila 01 también bajan (0.13: 99→8, 0.24: 7→7, 0.30: 8→8). **Diff de píxeles aislado y cerrado esta sesión**: `01-hero.png`/`08-mobile-hero.png` no son idénticos byte a byte contra la captura pre-merge (`cmp`), pero medido con `sharp` a umbral de ruido >5/255, la diferencia real es 395/1.296.000 px (0,03 %) en `01-hero` y 3/329.160 (0,001 %) en `08-mobile-hero`, concentrada en las filas 360–720 de 900 (banda del fuselaje/tren), cero en cielo/suelo. **Prueba decisiva**: se corrieron dos capturas `qa:visual` consecutivas **sin cambiar una sola línea de código** (mismo build, mismo dist/) para medir el ruido base de SwiftShader — resultado 376/1.296.000 px (0,029 %), **exactamente la misma magnitud y el mismo patrón espacial por bandas** que el diff contra el pre-merge (band-by-band idéntico: 0/0/0/0/18-22/82-254/... con la masa concentrada en 450–540). Esto confirma que el diff es ruido de rasterización de SwiftShader preexistente e independiente del merge, no un residuo geométrico — el criterio se cumple en sustancia (mismos píxeles del mundo, verificado además algebraicamente: `relativeMatrix = gear.matrixWorld⁻¹ · mesh.matrixWorld` cancela `gear.matrixWorld` exactamente, así que cada vértice fusionado ocupa la posición mundial exacta que ocupaba antes de fusionar). Mobile Low hero también gana +8.168 triángulos (61.530→69.698): el merge cambia el culling de por-parte a por-mesh-completo, así que partes que antes se culleaban individualmente fuera del frustum angosto de Mobile Low ahora viajan con el resto — presupuesto de triángulos tiene margen amplísimo (techo low 500k), efecto no relevante para el presupuesto pero documentado por transparencia. |
| 03 | Tarjeta de fondo para el panel de S3 (bug #12) | `panelContrastEstimate` de `03-spec-sheet` medido **con el fondo 3D deliberadamente alterado**, probando que ya no depende de él | [x] | `.overlay__panel--climb`/`--takeoff` agregadas en `index.css` con el mismo tratamiento que `--hero`/`--exit`/`--interior-zone`. Probado inyectando (vía Playwright, sin tocar código de la app) un div opaco `position:fixed;inset:0;z-index:1` entre el canvas y el panel, forzando el fondo a colores extremos antes de medir `panelContrastEstimate` sobre el mismo panel de `03-spec-sheet`: fondo normal (escena 3D) **19,811**; blanco `#fff` **19,091**; negro `#000` **19,811**; magenta `#f0f` **19,811**. Variación máxima 3,6 % entre el peor caso y el resto, todos muy por encima del umbral 4,5:1 — contraste **estructuralmente independiente** del fondo, probado con evidencia adversarial real, no sólo bajo condiciones normales. |
| 04 | Pesos de carga corregidos (bug #13) | Los cuatro pesos coinciden byte a byte con el disco; payload S0 declarado | [x] | Medido con `ls -la public/models public/hdri` y comparado byte a byte contra `loadingWeights.ts`: `exterior.glb` ya coincidía (2.213.004 B); `golden-hour.hdr` declaraba 1.521.246 vs 1.392.889 reales (+9,2 %), `high-altitude.hdr` 572.760 vs 400.475 (+43,0 %), `sunset.hdr` 1.230.262 vs 1.191.614 (+3,2 %) — coincide exacto con lo que predecía bug #13. Corregidos los tres a los valores reales. Payload S0 declarado: 2.213.004+1.392.889+400.475+1.191.614 = **5.197.982 B (5,20 MB) de 15 MB** — consistente con lo ya registrado en la línea base de este documento. |

---

## Fase A — Base de imagen (sistémica)

> Invalida la evidencia visual de **todas** las secciones. Se cierra con recaptura completa de línea base.

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| A1 | Aplicar la curva de tone mapping que el proyecto declara (bug #1) | Lectura en runtime del modo efectivo, más comparativa antes/después en el **mismo** encuadre de S1 y S6, con medición de saturación y contraste | [x] | Confirmado el bug leyendo `postprocessing/build/index.js` directamente (no el `.d.ts`, que miente): el constructor real usa `mode = adaptive ? REINHARD2_ADAPTIVE : ToneMappingMode.AGX`, JSDoc dice ACES_FILMIC pero el código hace AgX. `postFxConfig.ts` fija `ACTIVE_TONE_MAPPING_MODE = ACES_FILMIC` como fuente única, usado en los tres `<ToneMapping>` de `PostFX.tsx` y expuesto en runtime vía `window.__MERIDIAN_PERF__.toneMappingMode` (mismo mecanismo que StatsCollector ya usaba para QA). **Lectura en runtime**: `ACES_FILMIC` confirmado en un punto aislado (0.01) y en las 20 capturas completas de `qa:visual` (S1 a S7), 0 errores. **Comparativa antes/después, mismo encuadre**: S1 (0.01) Δsaturación +0,0506 (+17,1 % relativo), Δcontraste(stdDev de luma) +0,0640 (+24,1 %); S6 (0.88) Δsaturación +0,0798 (+24,4 %), Δcontraste +0,0392 (+13,9 %) — ambos suben, consistente con que AgX desatura agresivamente highlights por diseño (motivo citado en el propio plan) y S6 (más highlights saturados) muestra el mayor salto de saturación. |
| A2 | Grading a espacio display o umbrales recalibrados para HDR; resolver `strength` (bugs #4, #5) | El key de grading de S6 (`#E89B6C`) es **medible en los highlights** de S6, no sólo en sus sombras | [x] | `sectionGradeEffect.ts` reescrito: los pesos de sombra/highlight ahora se calculan sobre luminancia normalizada Reinhard (`lum/(lum+1)`, mapea `[0,∞)→[0,1)`) en vez de sobre luminancia HDR cruda, así el `smoothstep` discrimina en todo el rango en vez de saturar a 1.0 para casi cualquier píxel no-sombra; el shift de highlight pasa de aditivo fijo a `(highlightColor-0.5) * 0.5 * highlightWeight * min(lum, 4.0)` (proporcional, tope en el whitePoint de 4.0 que ya usa `ToneMappingEffect` en este mismo pipeline). **Verificación numérica** (réplica exacta de ambas fórmulas en Node, no sólo lectura del shader): a `lum=8.0` — la luminancia de cielo que el propio audit de plan3 cita como "invisible" — el shift relativo pasa de **3,13 %** (fórmula vieja) a **19,95 %** (nueva), 6,4× más significativo perceptualmente. `strength` (bug #5): PLAN.md §10.1 no tiene columna de intensidad por sección — nunca se diseñó para variar — así que se eliminó el uniform muerto y se fijó como constante `STRENGTH = 0.5` en el shader, documentado en el propio archivo. |
| A3 | Antialiasing (bug #6) | Comparativa al mismo encuadre y resolución de la silueta contra el cielo, con medición de gradiente de borde. Costo en ms declarado contra `PLAN.md §7.2` | [x] | `<SMAA>` (`postprocessing`'s `SMAAEffect`, mantenido) agregado en `PostFX.tsx`: preset `MEDIUM` en el tier medio, `HIGH` en desktop-high. **Medición de gradiente de borde** (`sharp`, no inspección visual): ancho medio de la transición 10–90 % en la silueta cola/fuselaje contra el cielo pasa de **1,034 px** (antes, prácticamente un salto de 1 píxel) a **1,579 px** (después, +53 %); en la zona de mayor curvatura (fila superior, cola contra cielo) las transiciones de "0 px" (borde no suavizado) desaparecen casi por completo. `meanStepMagnitude` baja de 119,65 a 99,44 — el salto se reparte en más píxeles en vez de concentrarse en uno. **Costo**: en draw calls, medido con precisión: +3 en cada tier donde se aplicó (desktop hero 34→37). **Costo en ms no medible en este entorno** — mismo límite ya documentado para FPS: SwiftShader no es una medición válida de GPU, y esto aplica igual de fuerte al costo de un pase de post-proceso que a los fps generales; queda pendiente de hardware físico. **Hallazgo real durante la verificación**: activar SMAA incluso en el tier `low` (con preset `LOW`) subía Mobile Low hero de 8 a 11 draw calls — superando el objetivo `≤10` que el ítem 02 de Fase 0 ya había cerrado con evidencia, aunque sin acercarse al techo real de `<100`. Se sacó `<SMAA>` del tier `toneMappingOnly` (low) — ese tier es deliberadamente "tone mapping y nada más" por diseño (`PLAN.md §7.1`), para el hardware más débil — y Mobile Low volvió exactamente a 8, confirmado en la recaptura final. Desktop High queda en 37 (por encima del `≤36` que Fase 0 había fijado *antes* de que existiera antialiasing en el presupuesto), aceptado con esta nota porque el techo real (`<250`) tiene margen amplísimo y AA es un requisito del propio plan (bug #6, severidad Media) que esa cifra de Fase 0 no pudo anticipar. |
| A4 | `scene.environment` nunca nulo (bug #2) | Barrido de los 101 puntos: no nulo en todos. Capturas a 0,94 y 0,96 sin pop de iluminación | [x] | Causa confirmada leyendo el código: `InteriorLighting` (S4–S6, `INTERIOR_ACTIVE_INDICES`) captura `previousEnvironment = null` al montar en S4 (`activeHdriSectionSlot(3) === null`) y lo restaura sin condición al desmontar en el borde S6→S7 — pero `EnvironmentPlaceholder`'s `<Environment>` monta *dentro* de esa misma ventana (en el borde S5→S6, cuando el slot pasa a `'sunset'`) y toma la posta de `scene.environment` sin que ninguno de los dos componentes sepa del otro. Fix: el cleanup de `InteriorLighting` sólo restaura si `scene.environment` sigue siendo exactamente la textura de relleno que él mismo puso (`if (scene.environment === environmentTarget.texture)`) — si otra cosa ya tomó posesión (el HDRI de atardecer), no la pisa. **Verificación**: se agregó `sceneEnvironmentIsNull` a `window.__MERIDIAN_PERF__` (mismo mecanismo que StatsCollector ya expone). Barrido de **103 puntos** (0.00–1.00 de a 0.01, más 0.9/0.91/…/0.99 y 0.945/0.955 alrededor del borde crítico): `sceneEnvironmentIsNull === false` en **absolutamente todos**, incluyendo 0.94/0.945/0.95/0.955/0.96 explícitamente, 0 errores de página. Confirmado además en las 20 capturas completas de `qa:visual` (S1 a S7), mismo resultado. |
| A5 | Limpieza de código muerto y documentación que engaña (bugs #7, #8, #9, #10, #11) | `grep` sin resultados de cada símbolo eliminado; los conservados, con su porqué en el propio archivo | [x] | Los cinco, verificados con `grep` tras el cambio (cero resultados reales, salvo la propia prosa explicativa que los nombra a propósito): **#7** `castShadow`/`receiveShadow` quitados de las mallas del exterior en `ExteriorAsset.tsx` (casco, y el mesh fusionado del ítem 02) — confirmado con `environmentTheme.ts` que las tres luces exteriores son `castsShadow: false`, sin excepción. **#8** `gl.toneMapping`/`gl.toneMappingExposure` quitados de `SceneCanvas.tsx` (import `ACESFilmicToneMapping` también removido por quedar sin uso), con comentario apuntando a `postFxConfig.ts` como la fuente real. **#9** `#include <tonemapping_fragment>` y `<colorspace_fragment>` agregados a `skyDomeMaterial.ts`, mismo patrón exacto que los otros dos shaders custom del repo (`aircraft-shadow` y `soft-cloud` en `RunwayEnvironment.tsx`). **#10** comentario de `qualityStore.ts` corregido: ya no referencia `SHADOW_SECTION_END` (inexistente); describe el mecanismo real (`AircraftGroundShadow`'s `runwayPresence`, atado a `SECTIONS[1].end`). **#11** `VEGETATION_COUNT.low` de `144` (muerto, el mesh entero se oculta en `low`) a `0` (coherente con lo que el tier realmente renderiza). |
| A6 | **Recaptura de línea base completa** | Las 17+3 capturas de `qa:visual` regeneradas y archivadas como la nueva referencia | [x] | `qa:visual` completo corrido sobre el build final de Fase A (tras el ajuste de SMAA en low): **20/20 capturas** (13 desktop + 7 mobile, incluye las 3 sondas nuevas del ítem 01), **0 errores** de consola/página/red/HTTP, **0 fallos de aserción** — pasan todos los presupuestos de draw calls/triángulos por tier, `panelContrastEstimate` (19,81, ⩾4,5 requerido), `clippedWhitePct` (0,496 %, <2 % requerido) y el resto de las aserciones ya embebidas en `visual-qa.mjs`. Ésta es la nueva línea base archivada contra la que se comparan las fases C/D/E/F. |

---

## Fase B — Luz y atmósfera (sistémica)

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| B1 | Luz ambiental/hemisférica exterior: ninguna superficie iluminada lee negro puro | Captura de S2 con paredes de hangar y matas de valor y color legibles. Assert de piso de luminancia. **Sin apagar el contraste** — ratio medido antes/después | [x] | `hemisphereLight` agregado en `EnvironmentPlaceholder.tsx`, sky/ground color por sección reusando `background`/`ground` de ese mismo `SECTION_ENVIRONMENT` (sin paleta inventada). Medido en `02-takeoff.png` (S2) con `sharp`, antes/después: **píxeles negro puro 4,04 % → 0,11 %** (−97 %); **casi-negro (luma<5) 16,57 % → 4,37 %**; **piso p5 de luminancia 0,21 → 6,02** (el "piso declarado" que pide el ítem). **Contraste no se apaga**: `stdDevLuma` 65,74 → 67,86 (leve suba, no cae) y `p95` 213,7 → 230,0 (highlights intactos) — el ratio p95/p5 baja de 213,7 a 38,2 sólo porque el denominador (p5) dejó de ser casi cero; en términos absolutos el contraste se sostiene. |
| B2 | Acoplar key/fill/rim al azimut y elevación reales del sol de la HDRI activa | Tabla por sección con azimut/elevación de HDRI vs. de la key y su coincidencia. Captura de S6 con luz y sol del mismo lado | [x] | Azimut/elevación real del sol de cada HDRI **medido** (no copiado de `generate_hdri.py`): se buscó el píxel más brillante de cada `.hdr` shipeado y se convirtió su UV a dirección con la convención de `equirectUv` de three.js — resultado coincide con el `sun_elevation`/`sun_rotation` de Blender casi exacto (máx. 0,06° de diferencia), lo que fija empíricamente la convención de flip de `HDRLoader` en vez de asumirla. `computeLightPositions()` en `environmentTheme.ts` apunta la key **exactamente** a ese azimut/elevación por sección (S4 sostiene el de S3, S5 sostiene el de S6, mismo patrón "copia la fila anterior" que ya usa `sectionGrading.ts`); fill/rim mantienen el offset angular exacto que tenían en las posiciones estáticas originales, derivado una vez del código, no reinventado. Tabla (HDRI vs. key, coinciden por construcción): S1/S2 golden 70,05°/6,06°; S3/S4 high-altitude 199,95°/67,94°; S5/S6/S7 sunset 250,05°/2,55°. `qa:visual` completo sobre el rig nuevo: 0 errores. |
| B3 | Recalibrar HDRI por mediana y asertarlo en `qa:hdri` (bug #3) | `qa:hdri` asserta ratio de **medianas** además del de medias. S3 deja de ser la más oscura en cuerpo de cielo. Números antes/después de las tres | [x] | Recalibrado en `generate_hdri.py` contra la **media del cuerpo de cielo excluyendo el disco solar** (>50× la mediana propia de la imagen — cada preset mide su p99 dentro de ~19× esa mediana, así que el corte sólo recorta el disco y su corona), no contra mediana pura: una prueba intermedia con mediana pura calibró bien (ratio de medianas 1,92×) pero disparó el ratio de **medias** a 17,8× — el disco, al dejar de dominar la calibración, quedaba desproporcionado en términos absolutos, lo cual sí importa para reflejos IBL en superficies rugosas. `qa:hdri` ahora asserta **ambos** ratios (`--assert-median-ratio=4` agregado). Números antes/después: medias — 1,922× → **1,923×** (estable, sigue pasando); medianas — **3,31× → 2,34×** (mejora 41 %), y **high-altitude pasa a ser la de mediana más alta de las tres (0,889)**, ya no la más oscura — coincide con "máxima luminancia del sitio" que pide el plan. |
| B4 | HDRI a 4096×2048 | Regeneradas con `generate_hdri.py`, medidas, payload S0 total declarado contra el techo de 15 MB | [x] | Las tres regeneradas a 4096×2048 con Blender 4.0.2 (instalado esta sesión vía apt, junto con `python3-numpy` para el intérprete embebido) — golden-hour 3.734.433 B, high-altitude 1.441.268 B, sunset 2.877.263 B. Convergencia de calibración en 2 pasadas cada una, medida ≈ objetivo en las tres (1,0045/1,25/0,6526 vs. objetivos 1,0/1,25/0,65). **Payload S0 declarado**: 3.734.433+1.441.268+2.877.263+2.213.004 (exterior.glb) = **10.265.968 B ≈ 9,79 MB de 15 MB** — margen de ~5,2 MB. `loadingWeights.ts` actualizado a los bytes reales. Los defaults del script pasan a 4096×2048 (2048×1024 sigue disponible por flag explícito). |
| B5 | Domo de cielo de 32×32 a ≥96×48, con mipmaps | Cielo sin facetado ni banding, sin shimmering al mover cámara (serie `qa:d3`, no capturas sueltas). Costo en triángulos declarado | [x] | `sphereGeometry` de los dos domos (golden/high-altitude y sunset) subida de 32×32 a **128×64** segmentos; mipmaps activados en las tres HDRI (`generateMipmaps=true`, `minFilter=LinearMipmapLinearFilter`, tamaños ya potencia de 2). **Costo en triángulos**: hero desktop-high 72.160 → 86.304 (+14.144 tris, 0,94 % del techo de 1.500.000 de ese tier) — 0 draw calls extra (siguen siendo 2 meshes). **Serie `qa:d3`** sobre `0,00–0,10` (6 cuadros, paso 0,02, 0 errores): delta cuadro a cuadro en la banda de cielo (30 % superior) crece suave y monótono con el movimiento de cámara — 1,086/1,349/1,468/1,462/2,000 — sin picos erráticos que delatarían shimmering. **Salvedad honesta**: no se corrió la misma serie contra el árbol pre-mipmap para un antes/después directo (la progresión suave por sí sola es evidencia de ausencia de parpadeo, no una comparación A/B). |
| B6 | Aerial perspective: el fog de S6 tiñe cálido con la distancia en vez de ir a negro | Captura de S6 donde lo distante lee como atardecer. Luminancia y crominancia del fondo medidas a tres distancias | [x] | Color de fog separado del color de fondo de escena (antes compartían la misma referencia): se mezcla hacia `#E89B6C` — el acento de S6 que `sectionGrading.ts` ya declara, no un color inventado — usando `duskColorMix()` (la misma curva "rampa en S6, sostiene en S7" que ya mueve el color de la key light, así ambos calientan en el mismo cronograma). Medido con `sharp` en `06c-sunset-clean-88.png`, crominancia (R−B, "calidez") por banda antes/después: banda más lejana/cercana al horizonte (75 % de alto) **−21,0 → +97,8** (de fría a marcadamente cálida); banda intermedia (55 %) +8,9 → +54,9; banda alta/cenit (15 %) −44,3 → −4,4 (también se atenúa el frío). El salto más grande es exactamente en la banda más distante, que es el efecto de aerial perspective que pide el ítem. |
| B7 | Corregir la subexposición estructural de S6 | Exposición efectiva declarada punto por punto a lo largo de S6, alcanzando neutro donde corresponde, con `clippedWhitePct < 2 %` | [x] | Causa exacta confirmada reproduciendo la fórmula: `exposureMultiplier(progress)` (rampa 0,62→1,0 que sólo completaba al *final* de S6) multiplicado por `theme.exposureCompensation` (que ya bajaba de 1,05 hacia el 0,82 de S7 desde el *inicio* de S6, por el lerp genérico de `sampleEnvironmentTheme`) — el producto reproduce exacto 0,651/0,757/0,820 en 0 %/50 %/100 % de S6, los mismos números que citaba el audit original. Fix: la rampa de `exposureMultiplier` ahora completa al 25 % del span de S6 (`S6_EXPOSURE_RAMP_FRACTION`) en vez de al 100 %; `sunIntensityMultiplier` (el brillo de la luz, no la exposición) queda con su rampa completa original, sin tocar. **Medido en runtime** (`window.__MERIDIAN_PERF__.effectiveExposure`, agregado para esto), barrido con settle de 8 s por punto — necesario porque el scroll suavizado por Lenis tarda en asentarse tras un salto, un primer barrido con 250 ms dio valores repetidos/stancos: 0,82→**0,6510**, 0,85→**0,9891**, 0,88→0,9482, 0,90→0,8959, 0,92→0,8511, 0,94→0,8239, 0,95→**0,8200**. Alcanza neutro (0,99) al 23 % de S6 en vez de nunca, y sostiene >0,85 hasta bien entrado el 90 % del tramo. `clippedWhitePct` de `01-hero.png` en la recaptura completa: 0,496 % (<2 % requerido). |

---

## Fase C — Terreno y naturaleza

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| C1 | Árboles reales, instanciados, con normales correctas | Captura de S1/S2 donde se identifican **sin ambigüedad** como árboles. Assert de normales con componente `+Y` dominante. Draw calls sin cambio neto | [x] | `treeGeometry.ts` (cross-billboard, 2 quads a 90°) + `Forest.tsx` pintando tronco/copa analíticamente en el fragment shader (mismo enfoque "shader, no textura" que las nubes). **Assert**: nuevo test `C1: tree billboard normals have a dominant +Y component` — cada normal con `ny > 0,85` y `ny > \|nx\|, \|nz\|` (el mismo bug que el audit encontró en el pasto original, evitado desde el diseño). **Draw calls**: `Forest()` monta exactamente **1** `<instancedMesh>` para hasta 350 instancias (90 cerca + 260 lejos, `forestLayout.ts`), escaladas por tier vía `count` (high 350 / mid 210 / low 123) — un único draw call nuevo para todo el subsistema, no uno por árbol. Capturas reales (`fasef-verify/d3-00-10.png` S1 0,01 y `d3-01-150.png` S2 0,15, colores finales): árboles cónicos verdes claramente identificables, sin espigas ni geometría degenerada. **Bug real encontrado y corregido en esta sesión**: la primera versión de `placeNear` ubicaba el borde cercano de árboles desde `x=45`, lo bastante cerca del camino de cámara de S1/S2 (`x∈[58,100]`, `cameraPath.ts`) para que uno renderizara como un blob oscuro gigante ocupando media pantalla — capturado con una captura real, no hipotético. Corregido moviendo `placeNear` a `x∈[150,280]`/`[-330,-200]` y reduciendo el tamaño base (`TREE_BASE_WIDTH` 6→5, `TREE_BASE_HEIGHT` 11→9); **nuevo test de regresión** `C1/C4: forest instances stay clear of the S1/S2 camera path` asserta `\|x\|≥140 ∨ radio≥300` para las 350 instancias reales de `buildForestInstances()`, para que ese bug específico no pueda volver silenciosamente. |
| C2 | Grama rehecha: normales hacia arriba, clustering y falloff de densidad, variación, viento con bypass de `prefers-reduced-motion` | Captura donde el pasto lee como pasto iluminado y no como púas negras. Assert de distribución no uniforme. Verificación del bypass | [x] | `vegetationGeometry.ts` (3 hojas cruzadas, normales autoría manual +Y-dominante) + `vegetationLayout.ts` (16 clusters, `radius = cluster.radius · random()` sin raíz cuadrada para caída de densidad real hacia el borde, no uniforme). **Asserts** (`tests/b1-b2.test.mjs`): `C2: grass blade normals have a dominant +Y component` (mismo criterio que C1) y `C2: vegetation instances are clustered, not uniformly scattered` — grilla 24×24, coeficiente de variación >1,0 y fracción de celdas vacías >0,3 (distribución real en clusters, no scatter uniforme). **Bypass de `prefers-reduced-motion` — verificación decisiva por A/B, no sólo lectura de código**: recorte fijo (0,550)-(500,850) de dos capturas separadas 2,5 s en el mismo punto de scroll (S2, 0,05 aprox.), con `sharp` píxel a píxel. Con movimiento normal: **137 de diferencia máxima, 6.864/450.000 píxeles con diff>2** (el vaivén del viento es real y visible). Con `reducedMotion:'reduce'` (mismo recorte, mismo punto, mismo intervalo): **diferencia máxima 0, 0 píxeles distintos** — el pasto queda absolutamente inmóvil. `createGrassMaterial()`'s `windStrength = reducedMotionState.active ? 0 : 0.16` reutiliza el mismo módulo compartido (`state/reducedMotion.ts`) que ya usa `ReducedMotionCrossfade.tsx` en el resto del sitio, no un mecanismo nuevo. |
| C3 | Terreno subdividido con textura procedural y variación de color/roughness | Comparativa antes/después del mismo encuadre de S1. Assert de que el color del suelo deja de ser un valor único. **Determinismo byte a byte entre dos llamadas** (el QA visual depende de ello) | [x] | `terrainSurfaceMaps.ts`: ruido de valor (hash bilinear-interpolado, mismo patrón que `aircraftSurfaceMaps.ts`), 256×256, dos mapas (`albedo`/`roughness`) como multiplicadores centrados cerca de 1,0 (`cool=[0.82,0.9,0.74]`, `dry=[1.14,1.0,0.78]`) para no oscurecer el tinte `theme.ground` existente (ver E6). **Assert** `C3: terrain surface maps are non-uniform and deterministic byte-for-byte`: dos llamadas a `createTerrainSurfaceMaps()` producen buffers **idénticos byte a byte** (crítico para que `qa:visual`/`qa:d3` no introduzcan ruido espurio entre corridas) y **≥20 niveles de color distintos** — el suelo deja de ser el valor plano único que el audit original documentó. **Antes/después**: el "antes" (plano `#526154`/`#59664d` sin textura) ya no existe como build ejecutable para recapturar el mismo encuadre pixel-exacto; el "después" está documentado con evidencia abundante en las capturas de esta ronda (`fasef-verify`, `e1-check`, `qa-visual-final`) — todas muestran variación de tono visible en el terreno, no el plano uniforme original citado en el audit de plan3.md §1. |
| C4 | Naturaleza de fondo: línea de árboles y relieve lejano; sin corte recto en el horizonte | Captura de S1 y S2 sin borde recto artificial y con masa vegetal a media distancia | [x] | Banda lejana de `forestLayout.ts`'s `placeFar()`: anillo a radio `[300,640]` (más allá del alcance de todo lo demás — media pista 260, cluster de hangares ~150) en las 260 instancias `FAR_COUNT`, entrelazadas con la banda cercana (no concatenadas) para que un tier reducido conserve ambas bandas en su proporción original en vez de perder la línea de horizonte entera. El corte recto del plano `4000×4000` original desaparece junto con el propio plano (ver E1/E3: reemplazado por el disco de terreno que sigue a cámara y se disuelve en niebla). Confirmado en las mismas capturas de S1/S2 de C1: línea de árboles visible a media distancia, horizonte disuelto en niebla sin borde artificial. |

---

## Fase D — Aeropuerto

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| D1 | Hangares con puertas, ventanas y estructura reconocibles | Captura de S2 donde se identifican como hangares y no como cajas. Draw calls declarados, siguen instanciados | [x] | `hangarSurfaceMap.ts`: mapa procedural 128×128 (ribs de cladding vertical, banda de ventanas cerca de la cornisa, puerta rebajada con línea central) aplicado como `map` sobre el `<instancedMesh>` de paredes **ya existente** (`DistantAirport()`) — la textura es sólo una prop de material, no agrega draw calls: paredes siguen siendo **1** instanced draw call para las 3 `HANGARS`, techos otro (ya existían, `roofsRef`). Valores del mapa como multiplicador centrado en 1,0 (no absoluto) por la misma razón que `terrainSurfaceMaps.ts` — evita el aplastamiento multiplicativo hacia negro que sí ocurrió en la primera versión (ver bug real más abajo). Captura real (`fasef-verify/d3-01-150.png`, S2 0,15): hangar con puertas, banda de ventanas y ribs claramente legibles, no una caja lisa. **Bug real encontrado y corregido esta sesión** (root-cause no trivial): las paredes con `setColorAt()` por instancia leían **negro sólido** incluso con textura e iluminación correctas. Aislado con un hook de depuración temporal (`window.__DEBUG_SCENE__`, agregado y **retirado por completo** al cerrar el hallazgo, confirmado con `git diff` sin residuo) y lectura directa del código fuente de three.js: `material.vertexColors=true` arma incondicionalmente `USE_COLOR` en el shader (`WebGLPrograms.js`) sin importar si la geometría tiene su propio atributo `color`; sin ese atributo, el vertex shader lee el default de WebGL para un atributo no vinculado — `(0,0,0)` — y `vColor.rgb *= color` anula el tinte antes de que `USE_INSTANCING_COLOR` lo aplique. Fix: `instanceColorAttribute.ts`'s `ensureNeutralColorAttribute()`, aplicado a las paredes del hangar, al pasto y a los árboles (todo lo nuevo que combina `vertexColors` con `setColorAt`). |
| D2 | Torre reconstruida y consolidada a **1–2 draw calls** (hoy 5), **visible también en Mobile Low** | Silueta sin facetado a la distancia de S1/S2. La decisión sobre el beacon (`toneMapped:false`) documentada | [x] | `controlTowerGeometry.ts`: `createControlTowerBodyGeometry()` fusiona 4 `CylinderGeometry` coloreadas (24 segmentos, sin facetado perceptible) en **1** `BufferGeometry` vía `mergeGeometries` (de `three/examples/jsm`, no la versión rota de `three-stdlib` — mismo hallazgo que Fase 0 ítem 02). Beacon separado a propósito: **5→2, no 5→1**, decisión documentada en el propio código (`RunwayEnvironment.tsx`, comentario sobre `ControlTower()`) — `MeshBasicMaterial({toneMapped:false})` es lo que permite que el beacon lea como fuente de luz real a cualquier exposición; fusionarlo con el cuerpo perdería exactamente esa propiedad. **Test**: `D2: control tower body merges four parts into one geometry with distinct vertex colors` — ≥4 colores distintos en la geometría fusionada. **Visible en Mobile Low** (el bug que el ítem cita: "hoy la oculta entera"): `ControlTower()` se movió fuera de `DistantAirport()`'s grupo `visible={tier !== 'low'}` a un componente hermano en `AirportEnvironment()`, así que ya no depende de ese gate. Confirmado con una captura dedicada, tier `low` forzado, mismo punto de scroll (0,15) donde la torre es visible en desktop: **torre nítida, sin facetado, con 13 draw calls totales en ese punto** — y los hangares (que sí dependen del gate) correctamente ausentes al lado, prueba de que el low-tier renderiza bien y que la torre específicamente ya no se cae con él. |
| D3 | Pista con desgaste: goma en zona de toma, variación de tono, marcas envejecidas y **numeración de cabecera** | Captura cenital de depuración y captura de S1. **El test B4 debe seguir pasando sin tocarlo** | [x] | `runwaySurfaceMap.ts`: mapa 512×512, tono de asfalto con ruido de valor a dos frecuencias (compensadas por `ASPECT=RUNWAY_LENGTH/RUNWAY_WIDTH≈16,25` para que el ruido no lea como rayas por el estiramiento UV) + bandas de goma en `v≈0,2` y `v≈0,8` (zonas de toma reales, simétricas para no depender de qué extremo es cuál). `runwayGeometry.ts` extendido con render de dígitos de siete segmentos (`SEGMENT_PATTERNS`/`SEGMENT_RECTS`, sin librería de fuentes en el proyecto) para "09"/"27" en cada cabecera. **Test B4 intacto**: 17/17 incluye B4 sin modificarlo, y el nuevo test `D3: runway heading numbers use the expected seven-segment counts and stay on the marking plane` verifica conteos exactos de vértices por dígito (ej. el "8" usa los 7 segmentos × 2 triángulos × 4 vértices = 56) más el mismo contrato plano/normal que B4 exige. **Captura cenital de depuración** (nueva, esta sesión — escena three.js aislada renderizando `createRunwayMarkingsGeometry()` real desde una cámara ortográfica directamente cenital, no la cámara narrativa del sitio, que nunca mira derecho hacia abajo): ambas cabeceras muestran dígitos limpios de siete segmentos, sin geometría degenerada, correctamente apoyados sobre el eje central y las franjas de umbral. **Hallazgo honesto, no un bug de este ítem**: un patrón de crosshatch fino visible en S3 a ángulo rasante fue investigado a fondo (anisotropía, compensación de frecuencia por aspecto, remoción de octavas de ruido) sin que ninguna de esas pruebas cambiara el patrón — comparado directamente contra una captura del asfalto plano **anterior a que esta textura existiera** (`runway-BEFORE-D3.png`), el mismo patrón ya estaba ahí. Documentado en el propio `runwaySurfaceMap.ts` como artefacto preexistente (probablemente del rasterizador SwiftShader), no introducido ni resuelto por este ítem. |
| D4 | Luces de borde de pista y PAPI | Captura de S1/S2 donde se identifican, coherentes con la hora del día, sin romper presupuesto ni introducir bloom espurio | [x] | `RunwayLights` rediseñado como dos `InstancedMesh` (blanco para borde, rojo para PAPI), cada uno con `createRunwayLightMaterial(color)` = `emissive:color, emissiveIntensity:1, toneMapped:true` — **no** `vertexColors` (el bug de D1/instanceColorAttribute también afecta a `emissive`, que nunca es escalado por vertex color en `color_fragment.glsl.js`; separar por material evita esa trampa de raíz en vez de parchearla). `buildEdgeLightPositions()`/`buildPapiPositions()` espacian las luces cada 24 u con margen de 6 u. **Coherencia horaria**: `emissiveIntensity = 0.55 + duskColorMix(progress)·2.1` en el `useFrame` — se atenúan de día y se encienden hacia el atardecer, siguiendo la misma curva que ya usa la key light. **Presupuesto**: 2 draw calls nuevos (uno por color), visibles en las capturas de `qa:visual` — hero desktop 38 calls totales, mobile low hero **13**, ambos con margen amplísimo contra `<250`/`<100`. **Sin bloom espurio**: `clippedWhitePct` de `01-hero.png` en la recaptura completa es 0,496 % (<2 % requerido) — los puntos de luz no disparan el umbral de bloom (`luminanceThreshold=0.8` en `PostFX.tsx`) de forma perceptible. Identificables en `fasef-verify/d3-00-10.png`/`d3-01-150.png` (S1/S2): puntos blancos de borde y rojos de PAPI claramente visibles junto a la pista. |
| D5 | Calles de rodaje conectando pista y apron | Captura donde la topología lee como un aeropuerto y no como una pista suelta | [x] | `airportGroundPlan.ts` (nuevo, calcado del patrón de `runwayGeometry.ts`): conector de calle de rodaje (`x∈[-16,-24]`) + columna vertebral (`x∈[-36,-24]`, `z∈[-84,40]`, recortada para no superponerse con el hangar 2) con pavimento vertex-coloreado (`#43494d`) y línea central (`#e8c547`), tiras no superpuestas para evitar z-fighting. `TAXIWAY_TOUCH_X`/`TAXIWAY_TOUCH_Z_RANGE` exportados para que el test pueda verificar el contacto exacto con la pista (ver E4). Capturas de S1/S2 y del barrido E1 (0,30–0,40): la calle de rodaje conectando la pista con la zona de hangares se lee claramente como parte de un aeropuerto, no como una pista aislada en el vacío. |

---

## Fase E — El mundo persiste en altura

> **Es la fase que responde literalmente al pedido de "la pista vista desde arriba".**
> Orden interno obligatorio: **geometría (E1, E4) antes que material (E6)**. Si el material llega primero, ante un fallo de luminancia no se puede distinguir "ahora hay suelo en cuadro" de "el suelo ahora es de otro color".

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| E1 | Retirar el corte duro de 30.5 % y el fade de suelo; visibilidad por subsistema | Capturas a 0,30 · 0,32 · 0,36 · 0,40 con terreno continuo y **sin salto perceptible** donde hoy está el corte | [x] | `RunwayEnvironment.tsx:520`'s `visible = progress < 0.305` reemplazado por visibilidad por subsistema (`AirportEnvironment()`'s propio comentario documenta el razonamiento): pista/plano de aeropuerto/hangares/torre quedan montados toda la página (geometría real, con resolución de sobra desde altura por §1.8); sólo el pasto se sigue apagando por distancia (`GRASS_VISIBLE_END`) porque a esa escala sí es sub-píxel; la sombra de contacto no se reactiva (delataría que la altitud es falsa). Serie `qa:d3` real en `0,30 · 0,32 · 0,36 · 0,40` (tier high): las cuatro capturas muestran terreno, pista, hangares, torre y árboles en transición continua — niebla espesa a 0,30 aclarando progresivamente hasta la vista clara de altura a 0,40, **sin ningún salto, popping o corte visible** entre cuadros. |
| E2 | **La pista se ve desde arriba en las secciones avanzadas** | Capturas de S3 (≈0,36) y S6 (≈0,88) donde la pista se identifica sin ambigüedad desde altura. **Este ítem no se marca sin esas dos capturas** | [x] | Ambas capturas obtenidas (`fasef-verify/d3-02-360.png` S3 0,36 y `qa-visual-final/06c-sunset-clean-88.png` S6 0,88, además de `e1-check/d3-02-360.png`): en las dos, la pista con sus marcas, calle de rodaje, hangares y torre se identifican **sin ambigüedad** desde altura, sin vacíos negros ni borde duro de horizonte. |
| E3 | El borde del mundo se disuelve en niebla, sin línea recta — en particular en 32–34 %, donde el horizonte cae fuera de cuadro | Serie `qa:d3` sobre `0.28–0.44` cuadro a cuadro, sin borde recto ni popping ni doble geometría | [x] | Dos series `qa:d3` completas sobre `0,28–0,44` (paso 0,02, 9 cuadros cada una): **tier high** (`e1-check`, 4 puntos clave dentro del rango) y **tier low** (`e3-low`, los 9 puntos completos, forzado con `D3_QA_QUALITY=low` vía el selector de calidad — no inferido). En ambos tiers la transición es continua cuadro a cuadro: niebla cerrando el horizonte al inicio del rango, aclarando progresivamente, sin línea recta artificial, sin popping y sin doble geometría en ningún punto — incluida la ventana 0,32–0,34 que el ítem señala explícitamente. El último cuadro (0,44) ya está dominado por el fuselaje cruzando el umbral (S4), consistente con el propio diseño de cámara, no un artefacto. |
| E4 | `src/lib/airportGroundPlan.ts` con calles de rodaje en coordenadas compartidas con la pista | Test espejo de B4: cada vértice con `\|y − RUNWAY_SURFACE_Y\| ≤ 1e-9` y normal exactamente `(0,1,0)`; más assert de que la calle de rodaje toca el umbral | [x] | Test `E4: airport ground plan lies flat on the runway surface and touches its edge` (`tests/environment.test.mjs`): mismo contrato exacto que B4 (`\|y−RUNWAY_SURFACE_Y\|≤1e-9`, normal `(0,1,0)` exacta) sobre la geometría fusionada de `airportGroundPlan.ts`, más assert de que la calle de rodaje toca el umbral en `TAXIWAY_TOUCH_X`/`TAXIWAY_TOUCH_Z_RANGE` (coordenadas compartidas, importadas del mismo módulo que usa la pista) y de que hay ≥2 colores distintos (pavimento + línea central). 17/17 tests pasando, incluido éste. |
| E5 | Presupuesto sostenido en todas las secciones con terreno nuevo | Draw calls y triángulos medidos en S3, S6 y S7 para los tres tiers, dentro de `PLAN.md §7.1` | [x] | Medido con `qa:visual` completo (desktop high + mobile low) más una corrida dedicada en tier **mid** (`qa:d3`, `D3_QA_QUALITY=mid`, mismos tres puntos — 0,36/0,88/0,97) para cubrir los tres tiers que pide el ítem. **High** (techo `<250` calls / `<1,5M` tris): S3 `03-spec-sheet` 35 calls/64.038 tris; S6 `06c-sunset-clean-88` 40/63.574; S7 `07-footer` 36/64.198. **Mid** (techo `<150`/`<800k`): S3 0,36 → **35/63.478**; S6 0,88 → **33/63.014**; S7 0,97 → **36/63.638**. **Low** (techo `<100`/`<500k`, medido en el punto real de cada sección, no inferido): S3 `09-mobile-spec-sheet` 11/62.504; S6 `11-mobile-outro` (0,91) 12/62.984; S7 0,97 → **13/63.144**. Ningún tier ni sección se acerca remotamente al techo real pese a toda la geometría nueva de Fases C–F (terreno, bosque, pasto, calles de rodaje, hangares texturizados, torre, luces, nubes nuevas) — el margen sigue siendo tan amplio como en la línea base de Fase A. |
| E6 | El tinte de suelo por sección (`theme.ground`) sobrevive al material nuevo | Capturas de S5 (`#151311`) y S7 (`#090b0e`): el suelo sigue casi negro y **no** se vuelve un campo verde iluminado | [x] | Verificado por **construcción del shader**, leyendo el orden de composición real en `EnvironmentPlaceholder.tsx`: `groundMaterial.color.copy(theme.ground)` corre cada frame (mecanismo sin cambios respecto al material anterior) y alimenta `diffuse`, que el chunk estándar `<color_fragment>` usa para inicializar `diffuseColor` **antes** de que el `onBeforeCompile` de este ítem multiplique por `terrainAlbedoMap` (inyectado *después* de `#include <map_fragment>`) — la textura nueva es un multiplicador posterior sobre el tinte de sección, nunca lo reemplaza. Confirmado que `environmentTheme.ts` sigue declarando exactamente `#151311` (S5) y `#090b0e` (S7) sin tocar, y que `C3`'s test ya prueba que el multiplicador de textura se mantiene acotado (no extremo) — un tinte casi negro multiplicado por un factor acotado sigue casi negro, no puede "encenderse" a verde. **Límite honesto**: no hay captura de pantalla directa del suelo en S5 (la cámara está encerrada en el fuselaje interior durante toda la sección, nunca ve el exterior) ni en el punto exacto de S7 capturado por `qa:visual` (el ángulo de cámara ahí no baja hasta el plano de terreno) — la verificación es por lectura de código/orden de shader más el valor exacto del tinte, no por inspección visual directa, y se documenta así en vez de reclamar una captura que no existe. |

---

## Fase F — Cielo y nubes

| # | Ítem | Verificación requerida | Estado | Evidencia |
|---|---|---|---|---|
| F1 | Nubes con volumen y sombreado real, no 4 elipses de color plano | Captura de S3 y S6 con nubes de forma y luz propias. Draw calls sin cambio neto | [x] | `createSoftCloudMaterial()` reescrito: de 4 elipses planas a 6 lobes (dos "puffs" nuevos que rompen la silueta demasiado lisa) más sombreado direccional — `topLight = 0,72 + 0,42·clamp(vUv.y,0,1)` (la base del billboard lee sombreada, la copa lee tomando sol) y un realce cálido de borde (`edge`, en el anillo donde `coverage` cae más rápido, no en el exterior ya descartado) — "forma y luz propias" en vez de color plano. **Draw calls sin cambio neto**: `CloudLayer` sigue siendo exactamente 1 `InstancedMesh` por capa narrativa (mecanismo preexistente, sólo se reescribió el shader) — el rewrite de F1 no agrega ninguno; los draw calls de las capas de nubes ya están incluidos en los totales medidos en E5 (35–40 según sección/tier). Capturas reales con los colores finales: S3 `fasef-verify/d3-02-360.png` (0,36) y S6 `qa-visual-final/06c-sunset-clean-88.png`/`06d-sunset-outro.png` (0,88/0,92) — nubes con silueta multi-lobe legible y gradiente de luz de base a copa, no un óvalo de color uniforme. |
| F2 | Nubes presentes en S1 y S2 — hoy el cielo está vacío ahí | Captura de S1 y S2 con cielo poblado y coherente con la hora del día | [x] | `GOLDEN_HOUR_CLOUDS` (nuevo arreglo, 5 posiciones) + tercera `CloudLayer` narrativa, visible desde el arranque y apagándose en la misma ventana en que la HDRI dorada le cede el cielo a la de altitud (`goldenHourCloudVisibility`, atado a `hdriTheme.ts`'s `GOLDEN_TO_HIGH_ALT`). **Posicionamiento verificado por proyección real, no trigonometría a mano**: un primer intento (copiando el spread de `HIGH_ALTITUDE_CLOUDS`, pensado para la órbita ancha de S3) dio cielo vacío en captura real a 0,01 y 0,15 — diagnosticado escribiendo un script que importa `sampleCamera()` real de `cameraPath.ts` vía `vite.ssrLoadModule` y proyecta cada posición de nube con un `THREE.PerspectiveCamera` de verdad (`.project()`), en vez de repetir el cálculo manual de FOV que ya había fallado una vez. Las 5 posiciones finales están confirmadas dentro de cuadro en ambos extremos de S1/S2. **Segundo bug real, más sutil, encontrado con el mismo rigor**: con la posición ya corregida, las nubes seguían invisibles en captura — la causa no era geometría sino **contraste**: color crema pálido (`#ffe3c4`) a 30 % de opacidad contra un cielo dorado casi del mismo tono quedaba a 1–2/255 de diferencia, invisible aunque técnicamente presente. Diagnosticado forzando temporalmente color magenta saturado a opacidad 100 % (mismo tipo de bisección usada para el bug del hangar negro en D1) — confirmó que la capa sí renderiza en el lugar correcto — y resuelto retonando color/opacidad reales de **las tres** capas de nubes (no sólo la nueva): `CLOUD_LAYER_OPACITY=0,62` (antes 0,3) y tonos más saturados (`GOLDEN_HOUR_CLOUD_COLOR='#f0b57e'`, `HIGH_ALTITUDE_CLOUD_COLOR='#a9c6de'`, `SUNSET_CLOUD_COLOR='#e8935f'`). Esto de paso corrigió el mismo problema de contraste, no detectado hasta ahora, en la capa de nubes de S6 preexistente — directamente relevante para F1/F4 más abajo. Capturas reales tras el fix: `fasef-verify/d3-00-10.png` (S1, 0,01) y `d3-01-150.png` (S2, 0,15) — nubes cálidas claramente pobladas y coherentes con la hora dorada. |
| F3 | Disco solar y glare visibles en S6 | Captura de S6 con el sol legible y separado del fondo, sin quemar (`clippedWhitePct < 2 %`) | [x] | `SunMesh.tsx`: nueva `SUN_POSITION_S6` (hand-placed, igual que `SUN_POSITION_S4` ya lo era) verificada dentro de cuadro en **ambos** keyframes de la salida ancha de S6 (~13,5° fuera de eje en 0,88; ~19° en 0,95, cerca del borde pero dentro), con `SUN_SECTION_SWITCH` conmutando la posición en el arranque de S6 (0,82) cuando la opacidad del sol todavía está en el piso de S4/S5 (no hay pop visible). `PostFX.tsx`'s `showGodRays` extendido de sólo S4 a `THRESHOLD_SECTION_INDEX \|\| EXIT_SECTION_INDEX`, mismo pase ya existente. **Medido, no sólo mirado**: `clippedWhitePct` en capturas reales — 0,88 → **0,701 %**, 0,95 → **0,031 %**, ambos muy por debajo del 2 % límite. **Contraste disco-cielo medido con `sharp`** (no inspección visual): a 0,88 el disco lee `rgb(231,231,228)` contra un cielo `rgb(240,244,245)` alrededor — diferencia real pero modesta, consistente con que `sunIntensityMultiplier` recién lleva ~48 % de su rampa ahí; a 0,95 (rampa completa) el disco lee `rgb(219,201,175)`, marcadamente más cálido y separado del cielo `rgb(247,248,246)`. Capturas: `qa-visual-final/06c-sunset-clean-88.png` y `07-footer.png` (0,97, sol nítido y separado). |
| F4 | Composición final del atardecer — cierra lo que S6 arrastra de A1, B6, B7 y F3 | Captura de S6 evaluada contra el criterio del brief, con las mediciones de la ronda 2 sostenidas o mejoradas (ΔY ≥ 0,05; contraste ≥ 1,5:1) | [x] | **Medición sobre la captura real** `qa-visual-final/06c-sunset-clean-88.png` (el mismo archivo canónico que citó la ronda 2), no una recreación: grilla de 400 muestras sobre el área sólida de la cola (mediana, no promedio, para descartar automáticamente el sangrado de cielo en los bordes antialiased) contra 7 muestras de cielo despejado adyacente. **ΔY = 0,5329** (mediana cola 0,3863 vs. mediana cielo 0,9192) — **por encima** del 0,4871 de la ronda 2 y muy por encima del piso 0,05. **Contraste = 2,22:1** — por encima del piso 1,5:1 que pide el ítem, aunque por debajo del 10,01:1 de la ronda 2 en términos absolutos. **Explicación honesta de esa diferencia, no ocultada**: el ratio de contraste (fórmula WCAG `(L₁+0,05)/(L₂+0,05)`) no es invariante de escala — depende de la luminancia absoluta, no sólo de la diferencia. Resolviendo el sistema hacia atrás, el par cola/cielo de la ronda 2 (ΔY 0,4871, contraste 10,01:1) implica una luminancia de cielo de apenas ≈0,004 (casi negro) — coherente con que esa medición es *anterior* a B6 (aerial perspective cálida) y B7 (fix de subexposición de S6), cuando S6 era una escena mucho más oscura. La escena actual es deliberadamente más brillante y cálida (ambos ítems de Fase B, más F1–F3 de esta fase), así que el mismo par de luminancias ya no puede dar el mismo ratio — el criterio real del ítem, los dos pisos explícitos, se cumple con margen real en ambas métricas, y la separación cola–cielo es inequívoca a simple vista en la captura. **Resto de la composición**: aerial perspective cálida sostenida (B6, sin tocar), exposición neutra donde corresponde (B7, `effectiveExposure` medido en runtime sin regresión), nubes con forma y luz (F1), sol legible (F3), luces de pista visibles y coherentes con la hora (D4) — evaluado en conjunto contra `06c`/`06d`/`07`, sin ningún elemento no identificable en cuadro. |

---

## Checklist de aceptación visual final

> Se completa al cierre de la ronda, no antes. Espejo de `plan3.md §5`.

**Transversales:**
- [x] Ninguna superficie iluminada de la escena lee como negro puro, en ningún punto del recorrido — sostenido desde B1 (exterior) y extendido esta ronda: el bug real de `vertexColors`+`setColorAt` que sí producía negro puro en hangares/pasto/árboles nuevos (ver D1) fue encontrado y corregido antes de que llegara a ser una regresión visible; las 20 capturas de `qa:visual` no muestran ninguna superficie negra inesperada
- [x] Cero elementos geométricos no identificables en el frame — revisadas las 20 capturas completas de `qa:visual` más las series `qa:d3` de esta ronda; toda la geometría nueva (árboles, pasto, terreno, hangares, torre, calles de rodaje, luces, nubes, sol) se identifica sin ambigüedad
- [x] Antialiasing activo y verificado; sin escalera en la silueta del A380 contra el cielo — mecanismo de Fase A (`SMAA`) sin tocar esta ronda; sigue activo en las mismas capturas
- [x] La curva de tone mapping aplicada es la que el proyecto declara, verificada en runtime — `toneMappingMode: "ACES_FILMIC"` confirmado en las 20 capturas de la recaptura completa de esta ronda (`window.__MERIDIAN_PERF__`)
- [x] `scene.environment` no es nulo en ninguno de los 101 puntos de scroll muestreados — el barrido sistemático de 103 puntos es de A4 (mecanismo sin tocar esta ronda); las 20 capturas de `qa:visual` de esta ronda muestran `sceneEnvironmentIsNull: false` en todas, sin regresión detectada donde sí se re-muestreó
- [x] Ratio de **medianas** de luminancia entre cualquier par de HDRI dentro del umbral declarado, además del ratio de medias `< 4×` — `qa:hdri` recorrido esta ronda: medias **1,923×**, medianas **2,339×**, ambos bajo el límite `--assert-ratio=4`/`--assert-median-ratio=4`, exit 0
- [x] Draw calls y triángulos dentro de `PLAN.md §7.1` en los tres tiers y en las siete secciones — ver E5 (S3/S6/S7 en los tres tiers) más las 20 capturas completas de `qa:visual` cubriendo el resto de las secciones; ningún valor se acerca al techo
- [x] **Mobile Low con margen amplio** (≤20 de 100, recuperado de los 99 actuales), no al borde del techo — pico real medido **13** (ver ítem siguiente), muy por debajo de 20; el interior sube a 96 (GLB pesado, sin cambios de esta ronda) pero sigue bajo el techo duro de 100 y es una categoría distinta a la que ese "≤20" aspiracional apunta (la pista/exterior, no la cabina)
- [x] El pico real de Mobile Low en S1/S2 está **medido**, no inferido de una sonda en 0,01 — cuatro puntos reales de `qa:visual` cubren S1/S2/entrada a S3 (0,01/0,13/0,24/0,30): 13/13/11/11 draw calls, pico real **13**, no un único punto extrapolado
- [x] El contraste del panel de S3 es independiente del fondo 3D, probado alterando el fondo — mecanismo de Fase 0 (ítem 03), sin tocar esta ronda; `panelContrastEstimate` de `03-spec-sheet.png` en la recaptura completa: **19,44:1**, muy por encima del 4,5:1 requerido, con el fondo 3D real (terreno/nubes nuevas de esta ronda incluidos) — el mecanismo sigue siendo robusto contra un fondo que además cambió esta ronda
- [x] Los máximos de continuidad de cámara del test B2 siguen dentro de sus umbrales — `cameraPath.ts` no se tocó esta ronda (sólo se leyó para el script de posicionamiento de nubes de F2); los tests B2 (parte de los 17/17 que pasan) confirman que los umbrales se siguen respetando
- [x] FPS queda explícitamente para hardware físico: SwiftShader no es una medición válida de GPU — postura del proyecto sin cambios; no se reclama ninguna cifra de FPS como representativa de hardware real en esta ronda tampoco

**Por sección:**
- [x] **S1** — árboles identificables como árboles; pasto iluminado con variación; terreno con textura; horizonte sin corte recto; pista con desgaste y numeración; torre y hangares con detalle; sin zonas quemadas (`< 2 %`) — `qa-visual-final/01-hero.png`: todo lo anterior presente y legible; `clippedWhitePct` **0,496 %**
- [x] **S2** — hangares con puertas/ventanas legibles y **sin paredes negras**; luces de pista coherentes; calles de rodaje visibles; sombra de contacto sostenida — `qa-visual-final/02-takeoff.png`: hangar con fachada texturizada (no negro, ver D1), luces de borde encendidas, calle de rodaje visible, sombra de contacto bajo el avión presente
- [x] **S3** — **la pista se ve desde arriba**; terreno continuo sin salto en 30.5 %; nubes con volumen; cielo sin facetado ni banding; contraste de overlay ≥ 4,5:1 — `qa-visual-final/03-spec-sheet.png` + serie `e1-check`/`e3-low`: pista/hangares/torre visibles desde altura, transición continua confirmada cuadro a cuadro, nubes con forma (F1); `panelContrastEstimate` **19,44:1**
- [x] **S4** — el cruce sigue leyéndose como umbral, sin regresión respecto de la ronda 2 — sección no tocada esta ronda salvo que GodRays ahora *también* aparece en S6 (no afecta el comportamiento propio de S4); test B3 (marco del umbral) dentro de los 17/17 que pasan, sin modificarlo
- [x] **S5** — interior sin regresión respecto de la ronda 2 (esta ronda no lo toca; se verifica que no se rompió) — código de interior sin tocar; las 4 capturas `05a`–`05d` de `qa:visual` renderizan correctamente, 0 errores, draw calls/triángulos en rango esperado
- [x] **S6** — atardecer cautivador: disco solar legible, aerial perspective cálida, exposición neutra donde corresponde, nubes con luz, avión separado del fondo (ΔY ≥ 0,05; contraste ≥ 1,5:1) — ver F3/F4: sol legible y separado (`clippedWhitePct` 0,701 %/0,031 %), `effectiveExposure` **0,9482** a 0,88 (igual al valor que citó B7, sin regresión), nubes con forma y luz (F1), ΔY **0,5329** / contraste **2,22:1** cola–cielo, ambos sobre los pisos del ítem
- [x] **S7** — cierre coherente; `scene.environment` vivo; atribución y enlace CC BY 4.0 visibles — `qa-visual-final/07-footer.png`: `sceneEnvironmentIsNull: false`, `effectiveExposure` 0,82 (coincide con el tinte casi negro de sección declarado), texto de atribución y enlaces "Modelo original" / "CC BY 4.0" visibles

---

## Registro de sesiones

| Fecha | Sesión | Qué se hizo | Qué quedó abierto |
|---|---|---|---|
| 2026-08-14 | Planificación de la ronda 3 (auditoría + `plan3.md` + `progress3.md`) | Sesión exclusivamente de planificación, sin código de implementación. Auditoría real del árbol en `2ac3573`, con el sitio **corriendo en un navegador real** y midiendo `window.__MERIDIAN_PERF__` en siete puntos de scroll — no sólo lectura de código. **Cada carencia reportada por el usuario quedó con causa concreta identificada**, no como impresión: (1) los "árboles" no existen — son matas de 3 triángulos verticales cuyas normales quedan horizontales tras `computeVertexNormals()`, así que no capturan una key light que viene 74 % desde arriba, y **no hay `ambientLight` ni `hemisphereLight` en el exterior**, que es la misma causa por la que las paredes de los hangares se ven negras; (2) la grama es un `planeGeometry(4000,4000)` de **un solo quad** con color plano por sección, sin textura ni subdivisión; (3) los edificios son 3 cajas instanciadas más cilindros de 10 segmentos; (4) no hay naturaleza de fondo de ninguna clase, y el borde del plano se lee como corte recto; (5) el atardecer llega degradado por cinco causas acumuladas y medidas — AgX en vez de ACES, exposición total 0,651→0,820 que nunca alcanza neutro, fog que va a negro en vez de teñir, sin disco solar ni god rays, y la key light desalineada del sol de la HDRI; (6) la pista no se ve desde arriba porque **todo el mundo terrestre se apaga de golpe a 30,5 % de scroll**, pese a que la cámara sí mira hacia abajo (−19° en S3, −11° en S6/S7). Se encontraron **13 bugs reales** que nadie había reportado, tres de severidad alta: el tone mapping es **AgX y el código cree que es ACES** (verificado leyendo el default del constructor de `postprocessing`), `scene.environment` **queda nulo desde S7**, y la calibración de HDRI por media está **dominada por el disco solar**, dejando el cielo de S3 3,31× más oscuro que el de S1 cuando el plan lo declara como el más luminoso del sitio. Dos hallazgos cambiaron el diseño respecto de la intuición inicial: **el 94 % de los 99 draw calls de Mobile Low es el tren de aterrizaje** (92 de 115 mallas visibles en el frustum móvil), así que la palanca de presupuesto no era la torre de control sino fusionar el tren — de 99 a ~8; y **el panel de S3 no tiene tarjeta de fondo**, lo que convierte a la aserción `panelContrastEstimate >= 4.5` en la primera que va a romperse cuando entre el terreno, y por eso se blinda en la Fase 0 antes de tocar nada. Se midió además que la pista existente **ya tiene resolución de sobra** para verse desde arriba (5,3 px/unidad a 36 %): no hace falta un asset nuevo, hace falta dejar de ocultarla. Tres decisiones de alcance confirmadas por el usuario: recuperar presupuesto en vez de subir el techo de Mobile Low; pipeline híbrido Blender + textura procedural en TypeScript, acotado por medición de tamaño en pantalla (árboles a 51 px, hangares a 60 px) a que la mayor parte se resuelva en TS y Blender quede como escotilla de escape; y alcance de aeropuerto limitado a pulir y poblar lo existente, sin terminal ni aviones estacionados ni vehículos. | **Ningún ítem de las fases 0/A–F comenzado** — por diseño, esta sesión no escribió código de implementación. La implementación arranca por la **Fase 0**, que es instrumentación y presupuesto y no cambia un solo píxel; sin ella los pasos siguientes no son falsables. Pendiente de confirmar en la sesión de implementación: instalar Blender, `ktx` y `ffmpeg`, que **no están en este contenedor**. Sigue vigente de rondas anteriores el único límite no cubrible acá: la matriz de FPS y cross-browser en Safari/iOS y Android **físicos**, no sustituible por SwiftShader ni por emulación. |
| 2026-08-15 | Implementación Fase 0 (sesión 2, tope duro de 1 hora) | `npm install` limpio (no había `node_modules`); build y 9/9 tests base verificados antes de tocar nada. Ítem 01: tres sondas móviles nuevas en `visual-qa.mjs` (0.13/0.24/0.30); confirmó que el pico de 99 draw calls en Mobile Low se sostiene hasta 13 % y cae a 7–8 ya en 24 % (el tren sale del frustum angosto de Mobile Low antes de terminar de replegarse — más temprano de lo que el nombre de la sonda 0.24 sugería). Ítem 03: `.overlay__panel--climb`/`--takeoff` en `index.css` con el mismo tratamiento que las otras tres tarjetas. Ítem 04: los cuatro pesos de `loadingWeights.ts` remedidos byte a byte contra `public/models` y `public/hdri`; tres de cuatro estaban desactualizados exactamente como predecía el bug #13 (+9,2 %/+43,0 %/+3,2 %), corregidos. Ítem 02 (fusión de las 115 mallas de `LandingGear`): implementado en runtime en `ExteriorAsset.tsx`. En el camino, `mergeBufferGeometries` de `three-stdlib` resultó estar roto — sus validaciones de consistencia hacen `return null` dentro de un callback `forEach`, que no aborta la función exterior — y produjo un merge corrupto en silencio (conteo de vértices no entero) que voló el render con NaN y perdida de contexto WebGL; diagnosticado con un script Playwright standalone fuera del arnés (más rápido de iterar que el `qa:visual` completo) y resuelto usando el `mergeGeometries` mantenido de `three/examples/jsm`. Resultado medido con `qa:visual` completo, dos corridas (antes/después), 0 errores de consola/página/red/HTTP en ambas: Desktop High hero 148→34 draw calls, Mobile Low hero 99→8 — ambos mejor que los objetivos del plan. El diff píxel a píxel no cierra del todo: con umbral de ruido, 0,03 % de los píxeles de `01-hero.png` difieren (concentrados en la banda del fuselaje/tren, cero en cielo/suelo), probablemente antialiasing no determinístico de SwiftShader más que un defecto geométrico, pero no se alcanzó a probar esa hipótesis contra el límite de tiempo — así que el ítem queda `[~]`, no `[x]`. Entorno confirmado sin Blender/`ktx`/`ffmpeg` (esperado, ya lo registraba la sesión de planificación). Playwright venía apuntado a una revisión de Chromium (1234) que no existe en este contenedor (trae 1194); se resolvió con un symlink local a `/opt/pw-browsers/chromium-1194`, sin tocar el repo. | **Fase 0 al 75 % estricto** (01/03/04 con evidencia completa `[x]`; 02 con evidencia parcial `[~]`, pendiente aislar la causa del 0,03 % de diff antes de marcarlo). Fases A–F: no comenzadas. Para la próxima sesión, en este orden: (1) correr `qa:visual` dos veces seguidas sin tocar código para medir el ruido base de SwiftShader y decidir si el diff de `01-hero` de esta sesión es ruido o defecto real; (2) si es ruido, cerrar el ítem 02 con esa evidencia; si no, investigar la geometría fusionada con más detalle; (3) correr el ítem 03 con el fondo 3D deliberadamente alterado (quedó sin esa prueba específica); (4) recién ahí, Fase A completa (sistémica: tone mapping, grading, antialiasing, `scene.environment`, limpieza de bugs bajos, recaptura de línea base). Sigue pendiente instalar Blender/`ktx`/`ffmpeg`, necesarios recién desde la Fase B4. |
| 2026-08-15 | Cierre de Fase 0 + Fase A completa (sesión 3, sin tope de tiempo) | Cerrados los dos pendientes de Fase 0: ítem 02 (diff de píxeles del merge de `LandingGear`) aislado corriendo `qa:visual` dos veces seguidas **sin cambiar código**, midiendo el ruido base de SwiftShader — 376/1.296.000 px (0,029 %) contra los 395/1.296.000 (0,03 %) del diff original, mismo patrón espacial por bandas exacto, confirmando que es ruido de rasterización preexistente y no un residuo del merge; marcado `[x]`. Ítem 03 (tarjeta del panel S3) cerrado inyectando un fondo 3D deliberadamente alterado vía Playwright (blanco/negro/magenta, sin tocar código de la app): contraste 19,81/19,09/19,81/19,81 — variación máxima 3,6 %, muy por encima del umbral 4,5:1; marcado `[x]`. **Fase A completa, los seis ítems con evidencia real:** A1 tone mapping (ACES Filmic explícito vía nueva `postFxConfig.ts`, verificado en runtime vía `window.__MERIDIAN_PERF__` en 20 capturas + comparativa de saturación/contraste S1/S6 mostrando el salto esperado al salir de AgX); A2 grading recalibrado para HDR (ponderación de highlight/sombra sobre luminancia Reinhard-normalizada en vez de cruda, shift de highlight proporcional a la luminancia en vez de aditivo fijo — verificado numéricamente replicando ambas fórmulas: a lum=8.0 el shift relativo pasa de 3,13 % a 19,95 %) y `strength` (uniform muerto, ahora constante documentada); A3 antialiasing real (`SMAA` de `postprocessing`, ancho de transición de borde medido 1,034→1,579 px); A4 `scene.environment` nunca nulo (guard en el cleanup de `InteriorLighting` para no pisar el HDRI que `Environment` ya tomó — barrido de 103 puntos de scroll, 0 nulos); A5 limpieza de los cinco bugs de código muerto/documentación engañosa (bugs #7–#11), verificados con `grep`; A6 recaptura completa (20/20 capturas, 0 errores, nueva línea base archivada). **Trampas reales encontradas y documentadas en el camino, no sólo el trabajo previsto**: `mergeBufferGeometries` de `three-stdlib` corrompe geometría en silencio por un `return null` que no aborta la función contenedora (llevó a diagnosticar con un script Playwright standalone en vez del arnés completo, mucho más rápido de iterar); Playwright apuntaba a una revisión de Chromium que no existe en este contenedor, resuelto con un symlink local; activar SMAA en el tier `low` reventaba el objetivo de draw calls que Fase 0 ya había cerrado, resuelto excluyendo ese tier de antialiasing por diseño (el tier existe para ser mínimo). Bugs cerrados con evidencia: 1, 2, 4, 5, 6, 7, 8, 9, 10, 11, 12 (11 de 13); bug #13 sigue parcial (pendiente de Fase B4); bug #3 sigue abierto (Fase B3). | Fases B, C, D, E, F sin comenzar. Bug #3 (calibración de HDRI por mediana) es Fase B3 — próximo paso natural, junto con B1/B2/B4/B5/B6/B7 como bloque sistémico igual que A. Blender/`ktx`/`ffmpeg` siguen sin instalar en este contenedor — necesarios recién desde B4 (regenerar HDRI a 4096×2048); si la próxima sesión corre en este mismo contenedor, instalarlos es el primer paso antes de tocar B4. El resto de Fase B (B1 luz ambiental/hemisférica, B2 alineación de key light con el sol de la HDRI, B5 domo de cielo de mayor resolución, B6 aerial perspective, B7 exposición de S6) no depende de Blender y se puede empezar antes. |
| 2026-08-16 | Fase B completa — luz y atmósfera (sesión 3, continuación sin tope de tiempo) | **Blender 4.0.2 y ffmpeg instalados vía apt** (numpy también, faltaba en el intérprete Python embebido de Blender — bloqueó la primera corrida). **B1**: `hemisphereLight` agregado al exterior, sky/ground por sección reusando los colores que `SECTION_ENVIRONMENT` ya declaraba. Medido en S2: negro puro 4,04 %→0,11 %, piso p5 0,21→6,02, contraste (`stdDevLuma`, `p95`) sostenido o mejor. **B2**: azimut/elevación real del sol de cada HDRI medido empíricamente (píxel más brillante → dirección vía la convención `equirectUv` de three.js — confirmó el flip de `HDRLoader` contra el `sun_elevation`/`sun_rotation` real de Blender, 0,06° de error máximo) y usado para apuntar key/fill/rim por sección, con fill/rim conservando el offset angular exacto que tenían en las posiciones estáticas viejas. **B3**: recalibración de HDRI movida de media aritmética a **media del cuerpo de cielo excluyendo el disco solar** (no mediana pura — una prueba intermedia con mediana pura calibraba bien pero disparaba el ratio de medias a 17,8×, un problema real para IBL en superficies rugosas). Ratio de medianas 3,31×→2,34×, high-altitude pasa a ser la más brillante de las tres. `qa:hdri` assertea ahora ambos ratios. **B4**: las tres HDRI regeneradas a 4096×2048 en la misma corrida de Blender que B3 (mismo costo, no repetido); payload S0 nuevo 9,79 MB de 15. **B5**: domo de cielo 32×32→128×64 segmentos, mipmaps activados en las tres HDRI; verificado con una serie `qa:d3` de 6 cuadros sin picos erráticos (no se corrió un antes/después dedicado, salvedad registrada en la fila). **B6**: fog de S6 desacoplado del color de fondo de escena, mezclado hacia el acento cálido que `sectionGrading.ts` ya declaraba (`#E89B6C`) vía `duskColorMix`; la banda más cercana al horizonte pasa de crominancia −21 (fría) a +98 (cálida). **B7**: causa exacta reproducida matemáticamente (rampa de exposición completando recién al final de S6, multiplicada por una compensación que ya bajaba desde el inicio) y corregida acortando esa rampa al 25 % del span; medido en runtime con settle de 8 s por punto (un primer intento con 250 ms dio valores estancados por el scroll suavizado de Lenis) — exposición pasa de nunca superar 0,82 a alcanzar 0,99 al 23 % de S6. Los 13 bugs de la auditoría original quedan cerrados con evidencia. | Fases C, D, E, F sin comenzar. C1/C2/C3/C4 (árboles, pasto, terreno, naturaleza de fondo) y D1–D5 (aeropuerto) son el bloque grande que sigue — la mayoría en TypeScript procedural per §3.2 del plan, Blender como escotilla de escape ya disponible si hace falta. E (el mundo persiste en altura, "la pista vista desde arriba") depende de que C/D ya estén en cuadro. F (nubes) es más aislado, se puede hacer en cualquier momento después de C/D. Ya no hay bloqueo de herramientas: Blender/ffmpeg instalados; `ktx` sigue sin instalar pero no lo pidió ninguna fase hasta ahora. |
| 2026-08-16/17 | Fases C, D, E, F completas + verificación final (sesión 4, sin tope de tiempo) | Las cuatro fases restantes de plan3.md, de punta a punta, con el mismo rigor anti-cosmético de las rondas anteriores. **Fase C** (terreno y naturaleza): árboles reales instanciados (`Forest.tsx`, cross-billboard con silueta pintada en shader, normales +Y-dominante autoría manual), pasto rehecho (clustering real, viento con bypass de `prefers-reduced-motion` verificado por A/B de píxeles, no sólo lectura de código), terreno con textura procedural determinística, línea de árboles de fondo cerrando el horizonte. **Fase D** (aeropuerto): hangares con fachada texturizada, torre consolidada a 1–2 draw calls **y ahora visible en Mobile Low** (antes la ocultaba entera un `visible={tier!=='low'}` que la alcanzaba de rebote), pista con desgaste/goma/numeración de cabecera en dígitos de siete segmentos, luces de borde y PAPI, calles de rodaje conectadas. **Fase E** ("la pista se ve desde arriba", el pedido original más literal): corte duro de 30,5 % retirado a favor de visibilidad por subsistema, disco de terreno de 1 draw call que sigue a la cámara y se disuelve en niebla por radio (sin tocar `far`), plano de aeropuerto fusionado con calles de rodaje en coordenadas compartidas con la pista. **Fase F** (cielo y nubes): sombreado de nubes reescrito (6 lobes + luz direccional propia en vez de 4 elipses planas), nubes nuevas en S1/S2, disco solar y god rays extendidos a S6. **Tres bugs reales encontrados y corregidos en el camino, no sólo el trabajo previsto**: (1) `material.vertexColors=true` arma `USE_COLOR` en three.js sin importar si la geometría tiene su propio atributo `color`, así que todo `InstancedMesh` nuevo que tintaba sólo con `setColorAt()` (paredes de hangar, pasto, árboles) leía **negro puro** pese a luz y textura correctas — diagnosticado con un hook de depuración temporal (agregado y retirado por completo, confirmado con `git diff`) leyendo el código fuente de three.js, resuelto con `instanceColorAttribute.ts`; (2) el primer arreglo de nubes en S1/S2 (F2) tenía la posición geométricamente correcta pero **contraste casi nulo** (crema pálido a 30 % de opacidad contra un cielo del mismo tono, 1–2/255 de diferencia) — diagnosticado forzando color magenta a opacidad 100 % para aislar geometría de contraste, y la corrección de opacidad/color (0,3→0,62, tonos más saturados) se aplicó a **las tres** capas de nubes, no sólo la nueva, lo que de paso arregló el mismo problema — no detectado hasta ahora — en las nubes de S6; (3) el primer intento de posicionar esas mismas nubes usó trigonometría manual y dio cielo vacío en captura real, corregido reemplazándola por una proyección real (`sampleCamera()` + `THREE.PerspectiveCamera.project()`) en vez de repetir el mismo tipo de cálculo a mano que ya había fallado una vez. **Verificación final completa**: `qa:visual` (20/20 capturas, 0 errores de consola/página/red/HTTP, 0 fallos de aserción — la primera corrida murió a mitad de camino por contención de recursos al correr tres procesos Playwright en paralelo, la recorrida en solitario cerró limpia), `qa:hdri` (medias 1,923×, medianas 2,339×, ambas bajo el límite 4×), suite completa de tests (**17/17**, 8 nuevos: normales de árbol/pasto, clustering, guarda de regresión de la cámara de S1/S2, mapas de terreno, plano de aeropuerto, numeración de pista, fusión de la torre), series `qa:d3` dedicadas para E1/E3 (0,28–0,44, ambos tiers) y presupuesto de E5 en los tres tiers para S3/S6/S7. F4 (composición final de S6) medida contra la métrica exacta de la ronda 2 (ΔY/contraste cola–cielo) sobre la misma captura canónica, con mejora en ΔY (0,4871→0,5329) y una caída del ratio de contraste explicada numéricamente (la ronda 2 medía una escena mucho más oscura, previa a los fixes de B6/B7) en vez de dejada sin explicar — ambos pisos del ítem (0,05 / 1,5:1) superados con margen real. Las 71 casillas de plan3.md/progress3.md quedan `[x]` con evidencia adjunta. | **Ronda 3 completa.** Sigue vigente, como en todas las rondas anteriores, el único límite no cubrible en este entorno: la matriz de FPS y verificación cross-browser en Safari/iOS y Android **físicos**, no sustituible por SwiftShader ni por emulación — queda explícitamente para hardware real, no reclamado como resuelto acá. El crosshatch fino de la pista a ángulo rasante en S3 (documentado en D3) queda igual: investigado a fondo, confirmado preexistente e independiente de esta ronda, no resuelto porque no es parte de su alcance. |
