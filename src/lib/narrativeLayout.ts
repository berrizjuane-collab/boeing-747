/** Land on readable shots, including the cockpit dwell rather than its approach. */
export const SECTION_DESTINATIONS = [0.06, 0.20, 0.355, 0.43, 0.526, 0.92, 0.98] as const

/** Local S2 reveal points. The first row appears shortly after entering S2. */
export const TAKEOFF_REVEAL_AT = [0.04, 0.32, 0.58, 0.8] as const
